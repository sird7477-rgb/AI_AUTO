# AI_AUTO Structural Weakness Backlog

## Language Note

This artifact is written in English to preserve continuity with the existing
GStack and structural-audit input artifacts used in this Ralph branch. Korean
remains the default for new strategy, architecture, and operational-judgment
documents; field names, state values, paths, and schema labels stay in English
where they are easier to reuse mechanically.

## Scope

This backlog materializes the priority TODO branch for structural audit items
1-5. It originally excluded:

- small-tool adoption review, candidate-tool scoring, helper wiring, and
  implementation
- guidance-budget warning cleanup and consolidation

Follow-up approval on 2026-05-28 reopened the read-only small-tool discovery and
adoption scoring slice only. The resulting no-install candidate matrix is
recorded in `plans/GSTACK_SMALL_TOOL_ADOPTION_CANDIDATES_2026-05-28.md`.
The broader non-GStack small-tool review is recorded separately in
`plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`.
Helper wiring for approved internal tools has since started. New package
installation, browser/runtime adoption, and broad guidance consolidation remain
outside this backlog's execution approval unless a new scoped plan explicitly
reopens them.

## Backlog Fields

Each item uses:

- `id`
- `slice`
- `severity`
- `evidence`
- `failure_mode`
- `blast_radius`
- `fix_type`
- `dependency`
- `status`
- `boundary_note`

Status values:

- `open`: identified backlog item with no scoped implementation started.
- `contract_started`: tracked pure contract or test coverage exists, but no
  runtime caller or fail-closed gate has been adopted.
- `complete_contract`: scoped repo-native contract plus any required caller,
  runtime wiring, or fail-closed gate exists; no active implementation work
  remains.
- `complete`: completed by implementation plus verification evidence.
- `complete_observe_mode`: observational capability is complete; policy
  promotion remains later-gated, not active.
- `display_only_complete`: functional behavior is complete; only optional
  presentation polish could be requested later.
- `installed_required`: installed and wired into the required verification path.
- `operational_clear`: real caller or user workflow is wired, runtime or
  fail-closed behavior is verified, related template/global/doc surfaces are
  synchronized, and the item must not appear in active or policy-attention TODO
  output.
- `advisory_contract`: report-only audit, CLI output, documentation, or test-only
  contract exists, but no fail-closed runtime caller owns the rule. This is a
  non-active status and must not be described as enforced.
- `reference_only`, `excluded`, `later_gated`: not active TODO statuses.

## P0: False Completion And Authority Leakage

`blocker` severity describes the structural risk class. It is not a current
blocker to this documentation-only Ralph branch unless the active micro-review
verdict cites it as unresolved.

| ID | Slice | Severity | Evidence | Failure Mode | Blast Radius | Fix Type | Dependency | Status | Boundary Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SA-P0-01 | Policy Authority / Review Gate | blocker | `docs/WORKFLOW.md`, `scripts/review-gate.sh`, `scripts/test-review-summary.sh`, `plans/AI_AUTO_STRUCTURAL_AUDIT_EXECUTION.md` | Degraded review or sidecar result is reported as normal approval. | Commit readiness, user trust, future automation claims. | contract + review discipline | none | complete_contract | P0 shell summary cases require normal trust only for multi-reviewer proceed, and `review-gate.sh` reports degraded coverage explicitly. |
| SA-P0-02 | Reflection Sidecars | blocker | `plans/AI_AUTO_REFLECTION_LOOP_V1.md`, `scripts/reflection_contracts.py`, `tests/test_reflection_contracts.py` | Reflection draft or knowledge sidecar appears to own completion, field truth, or Obsidian promotion. | Knowledge integrity and completion reporting. | contract + tests | Reflection contract surface | complete_contract | Sidecar failure remains warning-only; authority claims are rejected by contract tests. |
| SA-P0-03 | Reviewer Eligibility | blocker | `plans/AI_AUTO_REFLECTION_LOOP_V1_ENHANCEMENT_REVIEW.md`, `scripts/reflection_contracts.py`, `scripts/self_demo_contracts.py`, subagent review notes | Unanimous language is used without reviewer eligibility, context completeness, and degraded-state labels. | Ralph completion claims and review artifacts. | contract + acceptance tests | review summary semantics | complete_contract | Independent approvals require eligible context; degraded paths require explicit reporting. |

## P1: Source-Of-Truth And Artifact Drift

| ID | Slice | Severity | Evidence | Failure Mode | Blast Radius | Fix Type | Dependency | Status | Boundary Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SA-P1-01 | Template Lifecycle | high | `templates/automation-base/`, `tools/ai-auto-template-status`, `scripts/verify.sh`, `tests/test_template_global_contracts.py` | Template-owned, hybrid, and project-owned surfaces drift across docs, installer, doctor, status, and verify. | Template patch safety and downstream project updates. | manifest parity test | template ownership rules | complete_contract | Current tests lock manifest path existence, uniqueness, template version, patch notes, and template script parity for touched scripts within this lifecycle scope. |
| SA-P1-02 | Global Tools | high | `docs/GLOBAL_TOOLS.md`, `scripts/install-global-files.sh`, `scripts/automation-doctor.sh`, `scripts/bootstrap-ai-lab.sh`, `tests/test_template_global_contracts.py` | Helper added to one install/status/doc surface but omitted elsewhere. | Broken helper repair or misleading installation state. | helper parity test | helper registry surfaces | complete_contract | Current tests compare existing link surfaces and doctor/bootstrap reports required helper availability for the shipped global tool set. |
| SA-P1-03 | Artifact Sync | high | `plans/AI_AUTO_REFLECTION_LOOP_V1_ENHANCEMENT_REVIEW.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/todo-report.py` | Final answer contains material findings absent from the latest artifact. | Traceability, review quality, user decisions. | pure contract + delta tests + report wrapper | plan/TODO reconciliation | complete_contract | Current checker validates recorded finding metadata, and the TODO report wrapper fails verification when canonical TODO or policy-attention rows are present. |

## P1: Self-Demo Validation

| ID | Slice | Severity | Evidence | Failure Mode | Blast Radius | Fix Type | Dependency | Status | Boundary Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SA-P1-04 | Self-Demo | high | `plans/AI_AUTO_REFLECTION_LOOP_V1_ENHANCEMENT_REVIEW.md`, `plans/AI_AUTO_STRUCTURAL_AUDIT_PLAN.md`, `scripts/self_demo_contracts.py` | AI_AUTO upgrade is called ready without representative user-action evidence. | User has to manually validate every upgrade; hidden workflow regressions. | pure contract + demo schema | upgrade class definition | advisory_contract | Pure self-demo schema is retained as advisory/test-only evidence shape; no runtime caller enforces it. |
| SA-P1-05 | Self-Demo | medium | `scripts/verify.sh`, `scripts/review-gate.sh`, `tests/test_self_demo_contracts.py` | Demo evidence is confused with formal verification or review-gate approval. | Completion reporting and readiness claims. | pure contract + tests | verify/review authority | advisory_contract | Contract tests reject replacing `verify.sh` or `review-gate.sh`, but no runtime caller consumes the Python mirror. |
| SA-P1-06 | Benchmark Runtime Evidence | high | `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/benchmark-command.py`, `plans/benchmarks/`, `plans/AI_AUTO_BENCHMARK_BASELINE_2026-05-28.md` | Benchmark contract exists, but no representative workflow benchmark has been run, sampled, baselined, or recorded as evidence. | Performance readiness may be inferred from contract tests instead of measured runtime evidence. | measured baseline artifact + contract clarification | self-demo representative workflow | complete_observe_mode | Observe-mode capture exists with `hyperfine`; warn/gate policy is later-gated and not active. |

## P2: Structural Audit Evidence Quality

| ID | Slice | Severity | Evidence | Failure Mode | Blast Radius | Fix Type | Dependency | Status | Boundary Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SA-P2-01 | Verify Coverage | medium | `scripts/verify.sh`, `tests/test_structural_boundary_contracts.py`, `scripts/todo-report.py` | Broad shell fixture coverage is hard to audit for exact behavioral guarantees. | Maintenance cost and regression diagnosis. | audit marker test | changed behavior surface | complete_contract | Current tests lock critical verify fixture markers, and verify now checks canonical TODO plus policy-attention clearance. |
| SA-P2-02 | Rebuild And Split | medium | `tools/ai-rebuild-plan`, split helper docs, `tests/test_structural_boundary_contracts.py` | Read-only plan output is mistaken for approval to run write-capable refactors. | Broad code churn and behavior drift. | read-only boundary test | approved execution plan | complete_contract | Current tests check read-only wording and unchanged target file set; write-capable apply remains outside active TODO. |
| SA-P2-03 | GStack Follow-Up | medium | `plans/GSTACK_BENCHMARK.md`, `plans/GSTACK_BENCHMARK_RALPH_DEEP_DIVE.md`, `scripts/gstack_benchmark_contracts.py`, `tests/test_gstack_benchmark_contracts.py` | GStack reference language becomes mandatory process or duplicate authority. | Workflow bloat and authority conflict. | boundary regression test | benchmark boundaries | complete_contract | Runtime GStack adoption is excluded from active and later-gated TODOs; current tests reject installation, standing roster, and worktree execution from benchmark context. |

## Later-Gated Items

These are not active TODOs. They may become TODOs only if their trigger appears
and a new scoped plan records verification, rollback, and review coverage.

| Item | Trigger | Minimum Later Approval Gate |
| --- | --- | --- |
| Guidance Stage 2 consolidation | Real duplication pressure appears after primary/template budgets pass, or a human explicitly asks for consolidation. | Dedicated completion-pack skeleton plan, template version and patch notes, `./scripts/verify.sh`, and `./scripts/review-gate.sh`. |
| Self-demo runtime evidence | A specific AI_AUTO upgrade needs representative user-action evidence beyond pure contracts. | Dogfood plan with side-effect boundary, cleanup state, benchmark sample method, and proof that pure contracts caught at least one real or fixture workflow regression. |
| Benchmark warn/gate policy | At least several comparable observe-mode captures exist for the same command class and environment. | Baseline variance analysis, explicit threshold rationale, and proof that a failing benchmark cannot block unrelated correctness fixes. |
| High-risk relationship clearance | Authority leakage, startup preflight blocking, or unsafe vault writes need to be actively reduced before `ST-P1-08` or broader startup automation is promoted. | Implement only from `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md`, with authority-boundary tests, hard timeout/pass-through fixtures, vault write idempotency checks, and `verify.sh` plus `review-gate.sh` evidence. |
| Medium-risk relationship clearance | Review-context truncation, registry-scan expansion, template/helper parity drift, or degraded reviewer reporting causes repeated review friction or implementation risk. | Implement only from `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md`, with split-review command documentation, registry-only scan fixtures, template parity checks, degraded-trust reporting tests, and `verify.sh` evidence. |
| Low-risk relationship cleanup | Rebuild/split approval wording, guidance bloat, or display-only feedback mutation risks recur during later implementation. | Implement only from `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md`, with wording/static checks and targeted fixtures; run `verify.sh` before completion reporting. |
| Domain-pack retrospective promotion workflow | Multiple completed projects produce reusable domain-pack feedback, or a user asks to analyze one finished project's domain-pack experience for source-pack updates. | Create a scoped plan for a project-closeout feedback sequence that separates project-specific rules from reusable domain guidance, records sanitized evidence, updates only applicable `templates/domain-packs/<name>/` files, and verifies with `verify.sh` plus `review-gate.sh`. |
| MicroWork contract and review-gate audit | Repeated large or strategic AI tasks need enforceable micro-unit boundaries instead of prose-only guidance, or a user explicitly asks to implement MicroWork tooling. | Create a scoped plan for a side-effect-free `tools/micro-work` contract helper, thin `scripts/micro-check.sh` wrapper, targeted pytest fixtures, `verify.sh` smoke coverage, and report-only `collect-review-context.sh` audit integration. The tool must not become a runtime, queue, scheduler, UI, or completion authority above `verify.sh` and `review-gate.sh`. |
| Cross-runtime model-class routing enforcement | Sessions show the fast/low class (Codex GPT-5.3 Spark, Claude haiku, Gemini flash) is expected for lightweight or bounded work but delegation stays on the standard class, or a user explicitly asks to implement model-routing controls. | Create a scoped, provider-neutral plan (see `plans/AI_AUTO_MODEL_ROUTING_ENFORCEMENT_PLAN_2026-06-02.md`) that separates `fast_scan`, `low_cost_impl`, `standard_impl`, and `frontier_review` lanes resolving onto each principal's `fast`/`standard`/`frontier` class; adds visible principal/role/model/class/reason/fallback/confidence logging; audits missed fast-lane opportunities in report-only mode; reports `class_unavailable` honestly where a provider cannot honor a class; and introduces any bounded low-cost implementation role separately from `executor`. Do not globally downgrade `executor`, planner, verifier, or reviewer roles without runtime evidence and review-gate coverage. |
| Agent worktree registry | Multiple AI sessions repeatedly write in the same repo, or review-gate/tree-churn noise shows that manual worktree ownership is no longer enough. | Create a scoped plan from `plans/AI_AUTO_AGENT_WORKTREE_TOOL_REVIEW_2026-06-05.md` for a dry-run-first `ai-worktree status/start/finish/cleanup` helper that wraps existing `git worktree` use, reports ownership/stale state, and prints merge-time verify/review-gate checklists without auto-merge, auto-delete, scheduling, or completion authority. |
| Odoo view-inheritance registry-validation gate | A user asks to implement the Odoo view pre-validation gate, or another odoo.sh build breaks on a view inheritance/registry-load error that local static checks passed (queue item `odoo:view-inheritance-registry-validation-required`, open). | Implement only from `plans/ODOO_VIEW_INHERITANCE_REGISTRY_VALIDATION_FEASIBILITY_2026-06-09.md`. The highest-ROI domain-pack guidance slice is now promoted as `ST-P1-51` (advisory_contract): `verify-patterns.md` + `review-checklist.md` carry the "view inheritance is registry-validated, not XML-validated; XML parse pass ≠ installable" rule and the fail-closed `-u <module> --stop-after-init` gate with build-blocking-risk marking and alternative evidence. Remaining later-gated slices: a Docker/CI registry-load harness scoped to changed modules with odoo.sh module-set + point-release parity, and an advisory Tier-1 static lint. Must not make the Obsidian KB authoritative for schema/verification, must keep enterprise source mounted-not-baked, and must verify with `verify.sh` + `review-gate.sh`. |
| Framework UI vector-wireframe harness | Framework UI wireframes repeatedly miss visual fidelity by either redrawing from memory or embedding screenshots instead of recreating the UI as vectors. | Create a scoped plan for an `ai-visual-wireframe-check` style helper that requires reference screenshots, rejects embedded raster images in implementation-facing Excalidraw sources, validates required framework regions, and produces side-by-side comparison evidence without replacing human/AI visual review. |
| OCA static-lint adoption for the odoo domain-pack commit tier | A user asks to wire the static/commit validation tier, or recurring static-detectable errors (T1 deprecated/rename, T3 XML well-formedness, T6 duplicate field/label) keep reaching odoo.sh. From `ST-P1-54` open-source analysis (2026-06-10): the registry-load harness covers the push tier but no commit-tier static lint is wired. | Adopt off-the-shelf `OCA/pylint-odoo` + `OCA/odoo-pre-commit-hooks` (do **not** build a custom static lint — Article 1.1) as the commit tier feeding the existing push-tier `templates/domain-packs/odoo/validation-harness/` registry-load gate; add a pre-commit config plus a `verify-patterns.md`/`review-checklist.md` note on the commit→push→merge tiering, referencing `OCA/oca-addons-repo-template` for CI wiring. Pure domain-pack guidance + config; `verify.sh` + `review-gate.sh`. **Shipped as `ST-P1-55` (2026-06-10).** |
| Odoo 19 rename/removal cheat-sheet sourced from OpenUpgrade | A user asks to harden T1 migration-drift prevention, or T1 (renamed/removed field/model/API) recurs across builds. Ties to `plans/ODOO_BUILD_ERROR_ANALYSIS_2026-06-10/PREVENTION.md` candidate 1 (T1=15 cases, largest). | Add an advisory Odoo-19 rename/removal table to `templates/domain-packs/odoo/review-checklist.md` + `verify-patterns.md`, sourced from `OCA/OpenUpgrade` version-mapping analysis (e.g. `groups_id→group_ids`, `product_uom→product_uom_id`, removed `uom.category`/`uom_po_id`). `advisory_contract` guidance only — the registry-load harness stays the definitive check; the Obsidian KB stays non-authoritative per `ST-P1-49`. `verify.sh` + `review-gate.sh`. |
| Synthetic data-baseline for data-dependent validation (sandboxerp-style) | The harness's best-effort data-baseline gap causes repeated data-dependent false negatives (T5 demo, T7 NOT NULL) that odoo.sh catches but the 1-lite warm base misses. Low priority. | Evaluate a `getsandboxerp/sandboxerp`-style synthetic business-data seed layered onto the parity-pinned warm base (Odoo 19), keeping odoo.sh staging as the final validator. Scoped plan with no PII, regenerable-cache boundary (no hand-maintained state), and `verify.sh` + `review-gate.sh`. Non-OCA/unofficial source — reference only, not a dependency. |
| Findings-lane retrieval (close the PUSH→PULL loop) | Captured findings accumulate in the curated-findings lane (`Surfaces/<surface>`, `RepeatKeys/`) but `knowledge-retrieve` only searches the reference-baseline slim index, so harvested `surface: odoo` gotchas are never auto-surfaced during work; a user asks to make captured findings retrievable, or session-log mining produces enough findings that browse-only access is the bottleneck (initial trigger: 2026-06-12 jw_dev mining shipped 27 odoo findings none of which the hook can surface). | Extend `knowledge-retrieve` to also index the active domain's findings lane (e.g. `Surfaces/odoo` + `RepeatKeys/`) as a second, clearly-labeled ADVISORY routing source — never raw content, still capped, still fail-open. Must keep the reference-baseline lane authoritative-by-default and the findings lane advisory; must not make Obsidian authoritative for schema/verification per `ST-P1-49`; preserve the fail-open/silent-miss contract and `tests/test_knowledge_retrieve.py` heading-recall coverage; verify with `verify.sh` + `review-gate.sh`. **Recommended first slice (cheaper, lower risk): on-demand-only** — make `knowledge-retrieve` searchable over the findings lane when explicitly invoked, WITHOUT wiring it into the auto-inject `UserPromptSubmit` hook. Captures most of the value (keyword pull of captured gotchas on demand) at a fraction of the context-poisoning risk, since nothing is auto-injected into every prompt. Defer the auto-inject full version until the findings corpus is large enough (~50–100 notes) or a second domain project exists, then re-evaluate ROI. |
| Domain-pack merged-guidance drift visibility | A home-side domain-pack rule in `templates/domain-packs/<pack>/AGENTS.patch.md` or `WORKFLOW.md` is updated (e.g. 2026.06.12.0 added the slim-KB-first dev-loop step 0), but existing projects that already merged the earlier guidance get NO automatic signal that their hybrid `AGENTS.md`/`docs/WORKFLOW.md` is now behind the source pack. `ai-auto-template-status` tracks only the 46 base-template files; `ai-domain-pack status` detects drift on the installed `.omx/domain-packs/<pack>/` REFERENCE copy (manifest `template_version` + `source_root_hash`) but is a manual command and does not inspect the merged hybrid content — the merge into project instructions is deliberately manual per `docs/DOMAIN_PACKS.md`. Net: a project can silently run stale domain-pack guidance. (Observed 2026-06-12: jw_dev had been re-merged manually and is current, but nothing would have flagged it had it not.) | Do NOT auto-merge into hybrid files (preserves the manual-placement design and project customization). Cheapest scoped fix: surface domain-pack reference drift proactively — extend the existing `--install-codex-drift-notice` startup notice (which today covers only base-template drift) and/or auto-run `ai-domain-pack status` to also report when an installed pack's `source_root_hash`/`template_version` advanced since install, with a one-line "re-review merged `AGENTS.md`/`WORKFLOW.md` guidance against the updated `AGENTS.patch.md`" reminder. Report-only, no auto-write; `verify.sh` + `review-gate.sh`. |
| KB retrieval hook silently inert without project vault config | The domain-gated retrieval hook resolves the vault from the project's `.omx/local-config.json` (`obsidian.ai_auto_vault_dir`, via `knowledge-retrieve --repo <repo>`). A project can pass both gates (registered domain profile + domain-keyword prompt) yet inject NOTHING because no vault path is configured — fail-open silence indistinguishable from "no match", so the feature looks installed but is dead. (Observed 2026-06-12: jw_dev had the hook installed and `project-profile.json` domain=odoo but no `local-config.json`, so retrieval was inert until the vault path was added.) The contract tests pass because their fixtures configure a vault, so they cannot catch this. | Surface the dead-config case instead of failing silently: have `--install-kb-retrieval-hook` and/or `automation-doctor` / the codex startup notice WARN (report-only) when a project's profile is a registered KB domain but `.omx/local-config.json` lacks `obsidian.ai_auto_vault_dir`, pointing to the one-line fix. Keep the hook itself fail-open at runtime (never block a prompt); the warning belongs in install/doctor, not the hot path. `verify.sh` + `review-gate.sh`. |
| Intra-session reflexion enforcement | The intra-session anti-thrash reflexion rule shipped as WORKFLOW guidance only (2026.06.12.4). Guidance can be forgotten mid-loop, so it has no guaranteed firing — the same drift risk that let a stale runbook mislead an agent. Enforce only if thrashing is measured as a real recurring cost AND a reliable "same failure" signal exists. | Stage it, do not jump to a gate: (1) report-only OBSERVE — a session-scoped failure-signature ledger (`hash(check-command + error signature) -> count`) that logs when the same signature recurs >=2x, to measure real thrashing frequency; (2) if material, ADVISORY — inject a "write a reflexion note before retrying" reminder; (3) only if it persists, SOFT-BLOCK (fail-open, never hard-block a prompt). Detection is the hard part (false-positive on a genuine new failure after a real fix). Keep the hot path fail-open like the KB retrieval hook; `verify.sh` + `review-gate.sh`. |
| Context7 optional documentation lookup helper | External SDK/API work repeatedly loses time to manual documentation lookup, outdated examples, or nonexistent API usage; suggested threshold: at least 2-3 repeat findings or 5+ external API tasks per month. | Keep `Context7`/`ctx7` as an optional read-only documentation lookup candidate only, not a required verification/review dependency and not a standing MCP connector. Expected benefit is limited to external library/API tasks: roughly 5-15% task-time reduction and 20-40% fewer API/version mistakes when documentation is the bottleneck; whole-workflow impact is likely small. Measure with a short A/B sample before adoption; drop if matching errors or unnecessary calls become noisy. |

## Excluded / Reference-Only Items

These items must not appear in future active or deferred TODO reports. They are
kept only so future agents do not rediscover and re-propose them without a new
user request that explicitly reopens the risk.

| Excluded Item | Reason |
| --- | --- |
| Runtime GStack installation | Adds duplicate runtime authority, memory/state surface, and workflow overlap without a repo-local need. |
| Parallel sprint execution | Requires separate branch ownership, concurrency, and review hierarchy policy; current native subagent/team surfaces cover bounded parallel lookup. |
| Browser daemon / cookie / pair-agent paths | Persistent browser state, auth cookies, local HTTP, tunnel, or remote-agent access creates credential and session-boundary risk. |
| Ship/deploy/canary automation | Conflicts with explicit commit/push/deploy approval boundaries in this repository. |
| GBrain or second memory runtime | Duplicates Reflection/Obsidian/project-memory authority and adds sync/index permissions. |
| Autoplan/full external pipeline adoption | Duplicates ralplan/Ralph/structural-audit authority and would blur plan/run gates. |
| Bulk shell formatting with `shfmt` | High churn for low behavioral value; ShellCheck warning gate already covers actionable shell risk. |
| Markdown style lint as a required gate | Existing long-form operational docs would need many style exceptions and doc-budget already controls the actual problem. |
| Broad Semgrep adoption | Rule selection, false positives, and account-backed policy surfaces are out of scope for this local automation testbed. |
| `cloc`/`tokei`, `fd`, `yq` installation | Current repo has enough counting/search/YAML handling through existing shell, Python, `find`, `rg`, and `doc-budget.sh`. |

## Small-Tool Detailed TODO

These items capture the current detailed-review backlog from the GStack and
non-GStack small-tool candidate reviews. They are TODOs, not approval to
implement or install tools.

| ID | Item | Priority | Source | Status | Next Gate |
| --- | --- | --- | --- | --- | --- |
| ST-P1-01 | Review-gate short summary detail design | high | `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md`, `plans/AI_AUTO_LIGHTWEIGHTING_EXECUTION_PLAN_2026-06-05.md` U1.4 | display_only_complete | Fixture-based verdict parser design has a short summary contract and output, and the actual review summary/parser path is already the enforcing surface. Optional future polish is not active TODO. |
| ST-P1-02 | Untracked artifact review guard | high | `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, review-gate omission observation, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md` | complete_contract | Review context flags material untracked artifacts; review summary blocks unless content is included or manual review is reported. |
| ST-P1-03 | TODO/report normalizer | high | User TODO reconciliation concern, `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md`, `scripts/todo-report.py` | complete | `scripts/todo-report.py --fail-on-active` reads the canonical backlog and fails verification if active TODOs remain. |
| ST-P1-04 | Diff scope classifier | medium-high | `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md`, `scripts/review-gate.sh`, `plans/AI_AUTO_LIGHTWEIGHTING_EXECUTION_PLAN_2026-06-05.md` U1.4/U3.1/U3.2/U3.3 | complete | Review context reports scope, intensity, and required checks; `review-gate.sh` now consumes the generated scope summary before external reviewers run. Clear `docs`/`plans`-only diffs record `review skipped: docs-only` after verification, while scripts/templates/guidance/code/unknown or guarded diffs still run the full external review path. This is the U1.4 enforce promotion case retained from the audit; extra fail-closed wiring is intentionally closed as unnecessary. When no explicit untracked allowlist is set, review context derives one from tracked diff scope so unrelated untracked material is reported but not blocking. |
| ST-P1-05 | Benchmark auto-capture wrapper | medium-high | `plans/AI_AUTO_BENCHMARK_BASELINE_2026-05-28.md`, `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md` | complete_observe_mode | Optional JSON/Markdown capture path exists, `hyperfine` is installed after explicit approval, missing-tool fallback is tested, and benchmark evidence is recorded as observational data. |
| ST-P1-06 | Verify/review process leak guard | medium-high | Observed nested review-gate timeout and cleanup need, `plans/AI_AUTO_SMALL_TOOL_MICRO_EXECUTION_2026-05-29.md`, `tests/test_process_cleanup_runtime.py`, `plans/AI_AUTO_LIGHTWEIGHTING_EXECUTION_PLAN_2026-06-05.md` U1.4/U2.2/U2.3/U2.4/U4.2/U3.3 | complete_contract | Pure cleanup evidence contract exists and a deterministic timeout fixture validates reaped process evidence. Review-gate now marks in-gate verification with `AI_AUTO_IN_REVIEW_GATE=1`, clears review-context scoping env before internal verify, and calls `AI_AUTO_VERIFY_SCOPE=product` so the gate repeats only product pytest plus Docker smoke. Standalone `verify.sh` keeps full coverage by calling `verify-machinery.sh` before product smoke. U1.4 closure keeps additional force-wiring out of this path because template parity and review summaries are already enforced by their native gates. Review context also emits a report-only Tree Churn Audit when git status changes during context collection. |
| ST-P1-07 | Persona lens and review-gate enforcement | medium-high | User request on 2026-05-29, `plans/AI_AUTO_PERSONA_LENS_REVIEW_GATE_PLAN_2026-05-29.md`, `scripts/collect-review-context.sh`, `scripts/review-gate.sh`, `scripts/summarize-ai-reviews.sh`, `scripts/gstack_benchmark_contracts.py`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `tests/test_persona_lens_context.py` | operational_clear | Review context now emits active persona lenses, integrator requirement, review gate policy, and policy reasons from changed paths; review summary rejects omitted or malformed classifier fields; hard review/policy paths map to `strict_gate`, docs-only changes remain `verify_only`, UI files map to `review_gate`, and no standing persona roster or new reviewer process is introduced. |
| ST-P1-08 | Obsidian pending-output auto-push | medium-high | User request on 2026-05-29 after successful manual `knowledge-collect --push`, `docs/OBSIDIAN_INTEGRATION.md`, `tools/knowledge-collect`, `.omx/knowledge/drafts`, `plans/AI_AUTO_OBSIDIAN_AUTOPUSH_AND_UPDATE_VISIBILITY_PLAN_2026-05-29.md`, `scripts/install-global-files.sh`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `tests/test_template_global_contracts.py` | operational_clear | AI_AUTO home `codex` startup now runs a bounded pending-draft check across AI_AUTO plus registered projects, prints a visible Obsidian output notice, and stops at an explicit approval handoff command instead of pushing automatically. |
| ST-P1-09 | Project AI_AUTO update visibility | medium-high | User request on 2026-05-29, `tools/ai-auto-template-status`, `scripts/install-global-files.sh`, `docs/NEW_PROJECT_GUIDE.md`, codex drift notice integration, `plans/AI_AUTO_OBSIDIAN_AUTOPUSH_AND_UPDATE_VISIBILITY_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `tests/test_template_global_contracts.py` | operational_clear | The `codex` startup drift notice now emits a clearly framed AI_AUTO update-check block with state, project, status details, latest patch note, inspection command, and direct Korean patch action. |
| ST-P1-10 | High-risk relationship clearance | high | User request on 2026-05-29, `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md` risk levels, MR-1/MR-2/MR-6, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | High risks have enforced boundaries: completion authority blocks leakage, startup preflights are timeout-bounded warning-only pass-through, and vault writes require explicit config, existing guards, idempotent failure, and non-authority Obsidian semantics. |
| ST-P1-11 | Medium-risk relationship clearance | medium-high | User request on 2026-05-29, `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md` risk levels, MR-1/MR-2/MR-3/MR-4/MR-7, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | Medium risks have enforced boundaries: split/focused review is required for material untracked context, startup registry scans cannot workspace/mount crawl, template-owned changes require parity controls, and degraded reviewer reporting remains explicit. |
| ST-P1-12 | Low-risk relationship cleanup | medium | User request on 2026-05-29, `plans/AI_AUTO_MODULE_TOOL_RELATIONSHIP_REVIEW_2026-05-29.md` risk levels, MR-4/MR-5/MR-6/MR-7, `tests/test_structural_boundary_contracts.py`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | Low risks have enforced boundaries: rebuild/split planning is read-only until approved execution gates, guidance broad rewrites require an explicit plan and minimality evidence, and display-only notices cannot mutate feedback or apply changes. |
| ST-P1-13 | Planning visualization workflow promotion | medium-high | User request on 2026-05-29, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `docs/plans/_templates/excalidraw-spec-template.md`, `docs/plans/_templates/high-risk-diagrams.md`, `plans/AI_AUTO_NATIVE_WORKBENCH.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_visual_artifact_context.py` | operational_clear | Review context now emits a report-only visual artifact audit for `.excalidraw`, paired `*-spec.md`, stale exports, and ambiguous source ownership; Excalidraw-only input stays explanatory, unreviewed specs warn, reviewed paired specs become implementation-facing evidence, and no diagram tooling install is required. |
| ST-P1-14 | Product challenge planning gate | medium-high | `plans/GSTACK_BENCHMARK.md`, `plans/GSTACK_BENCHMARK_RALPH_DEEP_DIVE.md`, `docs/AUTOMATION_OPERATING_POLICY.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_product_challenge_context.py` | operational_clear | Review context now emits a report-only product challenge audit that requires a reason for broad/strategic/large work, caps lightweight questions at three, and records skip reasons for routine small tasks or already approved plans without replacing normal user approval or verify/review gates. |
| ST-P1-15 | UI/browser QA evidence workflow | medium-high | `plans/GSTACK_BENCHMARK.md`, `plans/GSTACK_BENCHMARK_RALPH_DEEP_DIVE.md`, `docs/UI_COMPLETION.md`, `docs/CHROME_CDP_ACCESS.md`, `plans/AI_AUTO_REFLECTION_LOOP_V1.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_browser_qa_context.py` | operational_clear | Review context now emits a report-only browser QA evidence audit with target, steps, screenshot note, CDP credential boundary checks, auto-fix blocking, redaction warnings, and visual-verdict authority warnings; targeted tests cover report-only, unsafe/safe CDP, auto-fix, redaction, and visual-only cases. |
| ST-P1-16 | Phase/scope guard workflow | medium | `plans/GSTACK_BENCHMARK_RALPH_DEEP_DIVE.md`, `plans/AI_AUTO_REFLECTION_LOOP_V1_ENHANCEMENT_REVIEW.md`, `docs/AUTOMATION_OPERATING_POLICY.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/collect-review-context.sh`, `scripts/summarize-ai-reviews.sh`, `scripts/test-review-summary.sh`, `tests/test_phase_scope_context.py` | operational_clear | Review context now emits a phase/scope guard section when `PHASE_SCOPE_PHASE` is set, and review summary blocks out-of-phase edits unless they are allowed, manually reviewed, or deferred with a path-plus-reason record; targeted shell and context tests verify the operating surface. |
| ST-P1-17 | Review finding revision loop | medium | `docs/MULTI_AI_COLLABORATION.md`, `scripts/review-gate.sh`, `scripts/summarize-ai-reviews.sh`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/test-review-summary.sh` | operational_clear | Review summary can now turn an explicitly accepted structured reviewer finding file into a bounded revision-task artifact; unclear review output, reviewer disagreement, repeated verification failure, missing changed diff, and cycle count above 2 produce stop/manual-review artifacts instead of automatic patch authority. |
| ST-P1-18 | Tool availability and adoption status workflow | medium | `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `scripts/automation-doctor.sh`, `scripts/bootstrap-ai-lab.sh`, `docs/GLOBAL_TOOLS.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `tests/test_template_global_contracts.py` | operational_clear | Existing `automation-doctor.sh` and `bootstrap-ai-lab.sh` command-status outputs now report tool adoption state and next gate for `shellcheck` and `hyperfine`; targeted tests lock the reporting surface and no install or silent promotion path is added. |
| ST-P1-19 | Completion-pack trigger and lens routing audit | medium | `docs/SECURITY_COMPLETION.md`, `docs/DEPLOYMENT_COMPLETION.md`, `docs/OBSERVABILITY_COMPLETION.md`, `docs/PERFORMANCE_COMPLETION.md`, `docs/DATA_COMPLETION.md`, `docs/UI_COMPLETION.md`, `plans/GSTACK_BENCHMARK_RALPH_DEEP_DIVE.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_GAP_AUDIT_2026-05-29.md`, `plans/AI_AUTO_WORKFLOW_PROMOTION_DETAIL_PLAN_2026-05-29.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_completion_pack_routing_context.py` | operational_clear | Review context now emits a report-only completion-pack routing audit with pack inventory, explicit input-shape trigger, file-scope trigger hints, GStack docs-generation reference-lens handling, and `runtime_lane_added: false`; targeted tests lock inventory, trigger, and no-runtime-lane behavior. |
| ST-P1-20 | Domain-pack retrospective feedback and source-pack promotion sequence | medium | User request on 2026-05-30, `docs/DOMAIN_PACKS.md`, `docs/DOMAIN_PACK_AUTHORING_GUIDE.md`, `docs/AUTOMATION_OPERATING_POLICY.md`, `tools/feedback-collect`, `.omx/feedback/queue.jsonl`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | `domain_pack_retrospective_policy` regularizes the sequence as a project-closeout-only gate: sanitized feedback is required, reusable vs project-specific findings must be separated, and project-specific rules cannot be promoted into reusable domain packs. No always-on domain-pack feedback loop or automatic template update is introduced. |
| ST-P1-21 | MicroWork contract and review-gate audit tooling | medium-high | User request on 2026-06-02 (activated 2026-06-03), `plans/AI_AUTO_MICROWORK_PLAN_2026-06-03.md`, `scripts/micro_work_contracts.py`, `tools/micro-work`, `scripts/micro-check.sh`, `scripts/collect-review-context.sh`, `scripts/verify.sh` | advisory_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): a micro-unit contract (`scripts/micro_work_contracts.py`) validates goal/scope_paths/smallest_useful_wedge/non_goals/required_evidence/completion_criteria and rejects scope↔non-goal conflicts; the side-effect-free `tools/micro-work` CLI (registered as a managed global helper) plus the thin `scripts/micro-check.sh` wrapper validate a unit and print a report-only scope-drift/non-goal-leak audit; `collect-review-context.sh` emits a self-contained, report-only `MicroWork Audit` that never blocks the gate. Contract + context tests and `verify.sh` smoke cover the valid, incomplete, conflict, and no-unit cases. It adds no runtime, scheduler, queue, UI, automatic execution, or completion authority. |
| ST-P1-22 | Cross-runtime model-class routing enforcement and observability (Codex/Claude/Gemini) | medium-high | User request on 2026-06-02, `plans/AI_AUTO_MODEL_ROUTING_ENFORCEMENT_PLAN_2026-06-02.md`, `docs/AI_MODEL_ROUTING.md`, `docs/AI_PRINCIPAL_RUNTIMES.md`, `scripts/discover-ai-models.sh`, `scripts/collect-review-context.sh`, `scripts/record-lane-decision.py`, `tests/test_model_routing_lanes.py` | complete_contract | Phases 0-2 landed and unanimously reviewed (Gemini + Codex with principal=claude, verify green): an observe-only Principal Class Lanes report, a report-only Model Routing Lane Audit, the four provider-neutral lanes, the `low_cost_impl` lane definition with a validated per-unit lane-decision recorder, and an evidence-driven tuning guard that keeps the standard, planner, verifier, and reviewer lanes at their current classes. Gemini is class-fixed via `agy`. The global Codex `executor-low` agent applies the reviewed lane definition. Normalization (2026-06-12, `plans/AI_AUTO_DELEGATION_RECORDING_NORMALIZATION_PLAN_2026-06-12.md`): per-unit lane-decision recording is now a standing rule — `AGENTS.md` § Delegation Recording Protocol (+ template mirror), `delegation_recording_policy` contract in `scripts/self_demo_contracts.py` + tests, `verify-machinery.sh` marker/parity locks, and `docs/AI_MODEL_ROUTING.md` § Evidence-driven tuning reworded so recording is a normalized step (recording carries observability only, not completion authority; spawn stays leader-judgment per `docs/AUTOMATION_OPERATING_POLICY.md`, no auto-dispatcher and no default class change). |
| ST-P1-23 | Principal misdetection fix (silent codex default) | medium-high | Discovered during ST-P1-22, `plans/AI_AUTO_PRINCIPAL_DETECTION_FIX_PLAN_2026-06-02.md`, `scripts/run-ai-reviews.sh`, `scripts/ai-principal-runtime.sh`, `tests/test_principal_runtime_contracts.py` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): valid launcher evidence selects the principal when `AI_AUTO_PRINCIPAL` is unset, an explicit selection contradicting valid evidence fails closed, defaulting to codex emits a visible notice, and evidence lookup plus workspace comparison anchor to the repo root with fixed-string matching. Tests cover selection, contradiction, the notice, and subdirectory launch/review consistency. |
| ST-P1-24 | doc-budget guidance-vs-content scoping revision | medium | User request on 2026-06-02, `plans/AI_AUTO_DOC_BUDGET_REVISION_PLAN_2026-06-02.md`, `scripts/doc-budget.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): the net-added budget measures cumulatively against the integration-branch merge-base, guidance scope is top-level only so content/spec docs in subdirectories are exempt (plus `DOC_BUDGET_EXEMPT_GLOBS`) consistently across the diff and the totals, duplicate detection covers template docs, and the escape hatch requires a recorded reason. Self-tests in `verify.sh` lock the accounting, exemption, and reason behavior; thresholds are unchanged. |
| ST-P1-25 | Review verdict parser prompt-echo hardening | high | Downstream ZURINI feedback `ai-auto:review-verdict-parser-prompt-echo` (2026-05-29), `scripts/summarize-ai-reviews.sh`, `scripts/test-review-summary.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `extract_verdict` skips code-fenced blocks, analyzes only the first real Verdict heading, and treats more than one distinct verdict token in that section (a prompt-echoed choice list) as ambiguous, yielding no verdict as a fail-safe, so an echoed prompt can no longer be read as reviewer approval. New `test-review-summary.sh` cases lock the prompt-echo choice list and the fenced-only verdict to single-reviewer `review_manually` rather than counting either as an approving reviewer; the template copies and patch notes carry the same change. |
| ST-P1-26 | agy prompt-only large-prompt placeholder guard coverage | high | Downstream JW odoo feedback `ai-runtime-adapter:agy-prompt-large-placeholder` (2026-05-24), `scripts/ai-runtime-adapter.sh`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): the agy/Gemini prompt-only fallback refuses an oversized `--prompt` against `RUNTIME_ADAPTER_PROMPT_ARG_MAX_BYTES` so a placeholder is never reviewed in place of the real prompt, and `verify.sh` now exercises both the small `--prompt` passthrough (the real prompt text reaches `--prompt` verbatim via a fake CLI) and the large-prompt refusal path. |
| ST-P1-27 | Archive double-confirm deletion safety default | high | Downstream ZURINI feedback `ai-auto:archive-confirm-delete-safety` (2026-05-29), `scripts/archive-omx-artifacts.sh`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `archive-omx-artifacts.sh --delete` alone refuses and exits non-zero before touching any files; destructive cleanup requires `--delete --confirm-delete`, and `--dry-run --delete` still previews removals. `verify.sh` covers refusal, dry-run preview, and confirmed deletion; README, `SESSION_QUALITY_PLAN.md`, `MULTI_AI_COLLABORATION.md`, and `CURRENT_STATE.md` describe the double-confirm as the recommended template safety default. |
| ST-P1-28 | Ralph completion-condition drift guard | high | Downstream ZURINI feedback `ralph:completion-condition-drift` (2026-05-31), `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py`, `AGENTS.md` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): the `completion_acceptance_scope` contract treats user-defined completion criteria as immutable acceptance scope and rejects an intermediate fail-closed safety gate (e.g. a no-order/no-candidate guard) as completion. A claimed completion must define required evidence and is accepted only when the deliverable is proven with every required evidence item, or when an explicit no-result final report carries every required item; mutated acceptance scope is rejected. Ralph Completion Discipline in `AGENTS.md` and the template copy state the same rule. |
| ST-P1-29 | Untracked review scope allowlist for targeted reviews | medium | Downstream JW odoo feedback `review-gate:untracked-scope-efficiency` (2026-06-01), `scripts/collect-review-context.sh`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `collect-review-context.sh` honors `REVIEW_UNTRACKED_ALLOWLIST` (comma/newline paths, directory prefixes, or globs) so a docs/spec-draft targeted review counts only matching untracked artifacts as blocking material; out-of-scope untracked files are reported but not blocking, and an empty allowlist keeps every material file in scope. Pairs with the existing `REVIEW_CONTEXT_DETAIL=light` path. `verify.sh` covers in-scope blocking, out-of-scope reporting, and the all-out-of-scope clear; README documents the targeted usage. |
| ST-P1-30 | Obsidian shareable-only auto-push from home | medium | User request on 2026-06-03, `tools/knowledge-collect`, `scripts/obsidian-autopush.sh`, `scripts/self_demo_contracts.py`, `scripts/install-global-files.sh`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `knowledge-collect --skip-disallowed-sync-class` pushes only shareable notes and reports skipped `local_private` (default strict abort unchanged); `obsidian-autopush.sh` resolves the vault from `.omx/local-config.json`, enumerates the home checkout plus registered projects, runs a secret/redaction preflight that fails closed, and is guarded to the home checkout; `obsidian_autopush_policy` adds a shareable-only lane where per-note shareable classification (plus a passing secret scan) is the publish consent, while any non-shareable push keeps the explicit approval requirement; publishing is invoked by running `obsidian-autopush.sh` on demand, and the home Codex startup notice lists waiting drafts plus the publish command without pushing automatically. `local_private` stays local by design. `verify.sh` covers the skip-push, leak refusal, idempotence, home guard, vault-not-configured skip, and secret fail-closed. |
| ST-P1-31 | Rule-based shareable auto-promotion for Obsidian publish | medium | User request on 2026-06-03, `tools/knowledge-collect`, `scripts/obsidian-autopush.sh`, `scripts/self_demo_contracts.py`, `AGENTS.md`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `knowledge-collect --auto-promote-shareable --promote-surfaces` reclassifies a `local_private` draft to `shareable_summary` only when its surface is on the allowlist and it is sanitized (collection already secret-scans via validate); `obsidian-autopush.sh` enables this by default with the AI_AUTO tooling-surface allowlist (`AI_AUTO_AUTOPROMOTE_SURFACES` override, `--no-auto-promote` opt-out), counts auto-promotable drafts as publish candidates, and fails closed on secret-like content; the `shareable_autopromotion_policy` contract encodes the default-deny rule (off-allowlist such as `ssh`, unsanitized, secret-like, or non-`local_private` are held local). The `옵시디언 푸시해줘` command keyword runs it. `verify.sh` covers promote-on-allowlist, hold-off-allowlist, and secret fail-closed; the capture default stays `local_private`. |
| ST-P1-32 | Guidance Stage 2 Slice A: root AGENTS.md budget restore | medium | User request on 2026-06-03, `plans/AI_AUTO_GUIDANCE_STAGE2_PLAN_2026-06-03.md`, `AGENTS.md` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): root `AGENTS.md` compressed 160→150 lines by tightening wording in place (no rule externalized, since the file is injected into the system prompt), clearing the doc-budget warning. Every guardrail stays explicit in the body (verify/review-gate enforcement, no-commit/push-without-approval, "바로 진행" cannot bypass approval gates, immutable completion-acceptance scope, all command keywords/Korean triggers, Completion Report Format); guardrail phrases verified present 1:1 and `verify.sh` AGENTS string greps pass. Home-only (root file; template `AGENTS.md` is separate). |
| ST-P1-33 | Guidance Stage 2 Slice C: large template-doc compression | medium | User request on 2026-06-03, `plans/AI_AUTO_GUIDANCE_STAGE2_PLAN_2026-06-03.md`, `docs/AUTOMATION_OPERATING_POLICY.md`, `docs/WORKFLOW.md`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | `guidance_stage2_consolidation_policy` promotes the safe decision boundary: Stage 2 requires an explicit user request plus a duplicate/consolidation report, and low-ROI edits remain blocked. The pilot measurement still stands; no broad policy/workflow compression is performed from the warning alone. |
| ST-P1-34 | Large reference baseline storage pattern (odoo #6 reusable part) | medium | Downstream JW odoo feedback `obsidian:large-reference-baseline` (2026-06-01), `docs/OBSIDIAN_INTEGRATION.md`, `templates/automation-base/docs/OBSIDIAN_INTEGRATION.md` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `OBSIDIAN_INTEGRATION.md` documents the reusable, project-neutral part of the odoo schema-validator request as a three-tier baseline pattern (`index` human note, `slim` compact JSON/CSV lookup, `full` source extract) with a pre-storage micro-consistency checklist (expected files, JSON/CSV parseability, count parity, lookup keys, reference integrity, duplicate keys, frontmatter rules, secret patterns) that treats schema-field keyword hits as advisory and fails only on copied secret values, and directs projects to keep one curated vault baseline referenced by baseline ID/source artifact/vault path instead of copying full exports per project. The project-specific schema validator stays project-owned by design. Root and template copies are byte-identical under template version `2026.06.04.1`. |
| ST-P1-35 | Reviewer sandbox-permission first-pass provisioning (avoid retry round-trips) | medium | User request on 2026-06-04, observed reviewer retry loop on sandbox/permission classification in `scripts/run-ai-reviews.sh` (REVIEW_RETRY_LIMIT, `network_or_sandbox`/`auth_or_permission` classify), `scripts/ai-runtime-adapter.sh`, `scripts/self_demo_contracts.py`, `tests/test_self_demo_contracts.py` | complete_contract | Review diagnostics now declare each external reviewer's first-pass read-only posture (`plan_permission_mode` for Claude, `sandboxed_prompt` for Gemini) without widening privileges; `reviewer_first_pass_permission_policy` locks that retry posture must match the first-pass posture and must not escalate write/network authority. |
| ST-P1-36 | Planning visual-artifact gate: complexity-triggered structurize/visualize/optimizer proposal | high | Downstream JW odoo feedback `planning:structure-visual-optimizer-gate` (2026-06-03), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `scripts/self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_self_demo_contracts.py`, `tests/test_planning_visual_gate_context.py` | advisory_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `planning_visual_gate_policy` proposes the structure model / flow visual / optimizer pass as candidates when any complexity signal (entangled state transitions, 1:N or bidirectional doc links, many permission/button/alert conditions, PDF/dashboard/migration scope, explicit tool mention) is present, keeps the source spec authoritative (rejects visualization override), and is advisory — proposing the missing artifacts satisfies it. A report-only `Planning Visual Gate Audit` in `collect-review-context.sh` mirrors the contract from `PLANNING_VISUAL_*` env signals and never blocks the gate; the planning guide documents the checkpoint. The planning guide is template-synced as of `2026.06.05.8`. Grouped with `ST-P1-37`. |
| ST-P1-37 | Planning visual-artifact gate: UI wireframe required for layout-heavy specs | high | Downstream JW odoo feedback `planning:ui-wireframe-required-for-form-heavy-specs` (2026-06-03), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `scripts/self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_planning_visual_gate_context.py` | advisory_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): the same `planning_visual_gate_policy` and `Planning Visual Gate Audit` treat any layout signal (form-structure change, section layout, list columns, popup view, button placement) as additionally requiring a UI wireframe distinct from the flow diagram, and the planning guide states a business/data flow Excalidraw is not a wireframe substitute. The guidance is template-synced as of `2026.06.05.8`; the audit remains advisory only. Shares the `ST-P1-36` contract surface. |
| ST-P1-38 | Spec ↔ code alignment gate inside the implement/review loop | high | Downstream JW odoo feedback `spec-gate:review-loop-alignment` (2026-06-04), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `scripts/self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_self_demo_contracts.py`, `tests/test_spec_code_alignment_context.py` | advisory_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `spec_code_alignment_policy` makes the spec-row to code-evidence mapping mandatory after a medium-or-larger patch or before applying a reviewer-suggested scope change, validates each row's class (aligned/updated/not_applicable/blocked/needs_user_confirmation), and surfaces `blocked`/`needs_user_confirmation` rows as report-only attention rather than auto-blocking. A report-only `Spec Code Alignment Audit` in `collect-review-context.sh` mirrors it from `SPEC_ALIGN_*` env signals — including rejecting an unknown patch size or a malformed/unknown-status row instead of reporting a false clear — and never blocks the gate. Extends artifact-sync (`SA-P1-03`) and `review_revision_loop` without duplicating them. |
| ST-P1-39 | Framework-native wireframe structure kernel (Odoo specifics stay project-owned) | high | Downstream JW odoo feedback `odoo:wireframe-must-follow-native-ui-structure` (2026-06-03), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `templates/automation-base/docs/PLANNING_VISUALIZATION_GUIDE.md` | complete | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `PLANNING_VISUALIZATION_GUIDE.md` adds the framework-neutral kernel — a UI wireframe mirrors the target framework's real view skeleton (inspect the existing view/screenshot first) and names its reference screen, keeping generic block diagrams separate. Odoo view specifics (control panel/statusbar/sheet/oe_title/smart buttons/notebook, dialog modal structure, list/kanban) stay project-owned per the odoo #6 precedent. Template-synced as of `2026.06.05.8`. |
| ST-P1-40 | Wireframe-authoring conventions kernel (Excalidraw runbook stays project-owned) | high | Downstream JW odoo feedback `planning:excalidraw-wireframe-guide-promote-to-ai-auto` (2026-06-03), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, project runbook `docs/runbooks/EXCALIDRAW_WIREFRAME_GUIDE.md`, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `templates/automation-base/docs/PLANNING_VISUALIZATION_GUIDE.md` | complete | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `PLANNING_VISUALIZATION_GUIDE.md` adds the generic wireframe-authoring conventions (source-of-truth naming, comment/arrow notation, mapped/hand-entered/read-only/hidden and shared-attachment notation, non-occluding arrow layout, save-time checklist). The Odoo-specific Excalidraw runbook stays project-owned. Template-synced as of `2026.06.05.8`; pairs with `ST-P1-39`. |
| ST-P1-41 | Preserve standard framework business flow before custom UI (kernel) | high | Downstream JW odoo feedback `odoo:preserve-standard-business-flow-before-custom-ui` (2026-06-04), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `scripts/self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_self_demo_contracts.py`, `tests/test_standard_flow_preservation_context.py` | advisory_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `standard_flow_preservation_policy` requires an impact map plus regression evidence before hiding or replacing a framework's standard business field behind custom UI, and flags a parallel-replacement-only custom field (sync-with or extend-standard passes). A report-only `Standard Flow Preservation Audit` in `collect-review-context.sh` mirrors it from `STANDARD_FLOW_*` env signals and never blocks the gate; the planning guide carries the kernel. The Odoo field/flow specifics (order_line, payment terms, invoice status, purchase→bill mapping) and any hard block stay project-owned. |
| ST-P1-42 | doc-budget plan/spec filename-label convention | medium | Downstream JW odoo feedback `doc-budget:plan-spec-label-exemption` (2026-06-04), `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`, `scripts/doc-budget.sh`, `scripts/verify.sh` | complete_contract | Implemented and unanimously reviewed (Gemini + Codex with principal=claude, verify green): `doc-budget.sh` exempts the `*.plan.md` / `*.spec.md` filename-label convention from the guidance-bloat budget by default (in both `doc_budget_is_exempt` and the cumulative-diff pathspecs) without any `DOC_BUDGET_EXEMPT_GLOBS` config, and prints their net-added volume on a separate `plan/spec labeled artifacts net added lines` line so it stays visible. Builds on `ST-P1-24`; a `verify.sh` self-test locks the exemption and the separate-total. Template-synced. |
| ST-P1-43 | Dedicated feedback-queue resolve tool | medium | User request on 2026-06-04, observed gap while resolving the JW odoo intake (`tools/feedback-collect` is read-only; resolution required hand-editing `.omx/feedback/queue.jsonl`), `tools/feedback-collect`, `tools/feedback-resolve`, `scripts/verify.sh`, `docs/GLOBAL_TOOLS.md` | complete_contract | `feedback-resolve` is now a dry-run-by-default global helper that discovers queues like `feedback-collect`, locks per queue, refuses unknown repeat keys and secret-like notes/sources, writes only with `--write`/`--apply`, and keeps repeated resolutions idempotent. |
| ST-P1-44 | Targeted review finding recheck loop | high | Downstream JW odoo feedback `review-gate:targeted-recheck-after-finding` (2026-06-04), `plans/AI_AUTO_FEEDBACK_QUEUE_REGULAR_PROMOTION_PLAN_2026-06-04.md`, `scripts/summarize-ai-reviews.sh`, `scripts/run-ai-reviews.sh`, `scripts/collect-review-context.sh`, `scripts/test-review-summary.sh`, `scripts/self_demo_contracts.py` | complete_contract | Review revision tasks now carry a targeted-recheck boundary and fail closed to manual/full review when changed scope expands. The targeted lane remains a loop accelerator only; it is not blanket approval or a replacement for final verify/review gates. |
| ST-P1-45 | UI verification micro-plan before CDP/browser evidence | high | Downstream JW odoo feedback `ui-verification:micro-plan-required-before-cdp` (2026-06-04), `plans/AI_AUTO_FEEDBACK_QUEUE_REGULAR_PROMOTION_PLAN_2026-06-04.md`, `scripts/self_demo_contracts.py`, `scripts/collect-review-context.sh`, `tests/test_browser_qa_context.py`, `docs/UI_COMPLETION.md` | advisory_contract | Browser QA evidence now requires a detailed UI micro-plan when detailed behavior verification is requested: layout, click targets, input handling, alerts/errors, sync/update behavior, and business mapping must each have evidence or be marked not-applicable before browser/CDP evidence is counted as sufficient. CDP remains optional and credential-equivalent. |
| ST-P1-46 | Deterministic installed domain-pack refresh workflow | high | User request on 2026-06-05, Claude/Gemini design artifacts, `plans/AI_AUTO_DOMAIN_PACK_REFRESH_PLAN_2026-06-05.md`, `tools/ai-domain-pack`, `scripts/install-automation-template.sh`, `tools/ai-rebuild-plan`, `scripts/verify.sh`, `docs/DOMAIN_PACKS.md`, `docs/GLOBAL_TOOLS.md` | complete_contract | `ai-domain-pack` now provides read-only status and dry-run-first refresh over ignored `.omx/domain-packs/` references using sidecar manifests for install-time baselines. New installs seed manifests, clean managed copies update mechanically, exact-match legacy copies can be adopted, local modifications/dirty legacy/unreadable manifests/experimental source branches fail closed, deliberately removed packs are not resurrected, and refresh never patches project `AGENTS.md`, `docs/WORKFLOW.md`, or `scripts/verify.sh`. |
| ST-P1-47 | Agent worktree registry/status helper | medium | User discussion on 2026-06-05, `plans/AI_AUTO_AGENT_WORKTREE_TOOL_REVIEW_2026-06-05.md`, current manual per-agent worktree operation, observed tree churn/review-gate friction | later_gated | Reference review captured workmux, Hive, Rift, git-stint, Etz, Orca, Arborist, Cuttlefish, and Verun. If repeated concurrent-agent churn appears, consider a repo-native dry-run-first `ai-worktree` helper for status/start/finish/cleanup over existing `git worktree` usage. It must not auto-merge, auto-delete, schedule agents, replace tmux/OMX, or claim completion above `verify.sh` and `review-gate.sh`. |
| ST-P1-48 | Framework UI vector-wireframe harness | high | User discussion on 2026-06-05 about Odoo Excalidraw drift, `docs/PLANNING_VISUALIZATION_GUIDE.md`, `templates/automation-base/docs/PLANNING_VISUALIZATION_GUIDE.md`, current project incidents where screenshots were omitted or embedded instead of vector-redrawn | later_gated | The guide now states that real framework UI wireframes must use screenshots only as reference artifacts and must redraw the implementation-facing Excalidraw source with vector elements; that guidance is template-synced as of `2026.06.05.8`. The remaining later-gated candidate is only the optional scoped `ai-visual-wireframe-check` helper: if the miss repeats, consider a helper that requires reference screenshots, rejects `image` elements/files in the final `.excalidraw`, checks required UI regions, and emits side-by-side comparison evidence. The tool must not replace visual review or treat screenshots embedded in the wireframe as valid completion. |
| ST-P1-49 | Official docs baseline retrieval and validation pattern | medium | User request on 2026-06-05 to audit Obsidian-stored Odoo official docs/manual/schema data, `Odoo19_Docs_KB` vault baseline `odoo-19-docs-2026-06`, `docs/OBSIDIAN_INTEGRATION.md`, `scripts/validate-odoo-docs-kb.py`, `tests/test_odoo_docs_kb_validator.py` | complete_contract | Official product documentation baselines got the reusable retrieval rule: project-authored guide first, official `slim` topic as navigation-only lookup, one matching `raw` topic for exact semantics, source URL fallback for freshness, and an initial end-user manual index-only/on-demand rule. The end-user manual storage part was superseded by `ST-P1-52`, which mirrors indexed user manuals into raw/slim tiers. The non-authority boundary remains unchanged: Obsidian does not decide project schema, verification, review, queue resolution, completion, or upstream freshness. Template docs were synchronized as of `2026.06.05.9`; the current template baseline is tracked by later rows. |
| ST-P1-50 | External Gemini review sandbox / WSL execution path | high | Downstream `Project_corini` feedback `review-gate:external-gemini-runner-wsl-docs` (2026-06-07), `plans/AI_AUTO_EXTERNAL_GEMINI_RUNNER_WSL_ANALYSIS_2026-06-09.md`, `scripts/ai-runtime-adapter.sh`, `scripts/verify-machinery.sh`, `docs/WORKFLOW.md`, `docs/MULTI_AI_COLLABORATION.md`, `docs/AI_MODEL_ROUTING.md` | complete_contract | Implemented and unanimously reviewed (Gemini approve + Codex architect approve_with_notes; principal=claude self-skip, `verify.sh` green): `agy_command_sandbox_ok` drops `--sandbox` for the raw `gemini` CLI when no usable container runtime (Docker/podman daemon reachable via a timeout-bounded `info` probe, not mere CLI presence) or macOS Seatbelt is available so external/WSL/desktop review runs no longer die on a missing Gemini sandbox image, honoring case-insensitive `GEMINI_SANDBOX=0|1`; `agy` and other wrappers keep `--sandbox`. `docs/WORKFLOW.md` (+ template) and `docs/MULTI_AI_COLLABORATION.md` document the external/Claude-unavailable path: `RUN_CLAUDE_REVIEW=0`, the `REVIEW_EXECUTION_MODE=external` runner, and the raw-`gemini` degraded-last-resort caveat. Recommending `GEMINI_REVIEW_COMMAND=gemini`/`RUNTIME_ADAPTER_AGY_COMMAND=gemini` (from the source item) was rejected because it makes the invocation `gemini --model …`, breaking the agy-only class-fixed routing in `docs/AI_MODEL_ROUTING.md`; `agy` stays the default. Template-synced as `2026.06.09.0`; a `verify-machinery.sh` self-test locks the sandbox gating. |
| ST-P1-51 | Odoo view-inheritance registry-validation gate (domain-pack guidance slice) | high | Downstream JW odoo build 33238937 invalid `account.view_in_invoice_tree` `state` selector that passed local XML parsing then failed odoo.sh registry load; queue item `odoo:view-inheritance-registry-validation-required` (resolved); `plans/ODOO_VIEW_INHERITANCE_REGISTRY_VALIDATION_FEASIBILITY_2026-06-09.md`; `templates/domain-packs/odoo/verify-patterns.md`, `templates/domain-packs/odoo/review-checklist.md` | advisory_contract | Shipped the highest-ROI slice (pure domain-pack guidance, no runtime dependency): `verify-patterns.md` + `review-checklist.md` now state "view inheritance is registry-validated, not XML-validated; XML parse pass ≠ installable" and promote `-u <module> --stop-after-init` to a fail-closed gate for changed addon view XML, marking runtime absence as **build-blocking risk** (not a clean skip) and requiring the feasibility §7 alternative evidence plus an explicit `odoo-view-registry: NOT validated locally` commit marker. This is non-authoritative onboarding guidance in a domain pack, not a mainline fail-closed caller, so it is `advisory_contract`, not enforced. Reliability still requires odoo.sh module-set + point-release parity; the optional CI/Docker registry-load harness and Tier-1 static lint stay later-gated; the Obsidian KB stays non-authoritative for schema/verification per `ST-P1-49`. No `templates/automation-base/` copy of this pack exists, so the `AI_AUTO_TEMPLATE_VERSION` template-sync marker does not apply (the `verify.sh` template-parity check passed). |
| ST-P1-52 | Odoo user-manual raw/slim Obsidian baseline | medium | User request on 2026-06-10 to collect Odoo user manuals into Obsidian, verify Obsidian guidance strength, and promote the rule; `Odoo19_Docs_KB`, `docs/OBSIDIAN_INTEGRATION.md`, `scripts/collect-odoo-docs-kb.py`, `scripts/validate-odoo-docs-kb.py`, `tests/test_odoo_docs_kb_validator.py` | complete_contract | User-manual pages listed by the Odoo 19 vault index are mirrored into `user-manual/raw` and `user-manual/slim` so routine business-flow and UI-step lookups do not require live fetches. The reusable guidance now says Odoo work must consult the local Obsidian baseline when `AI_AUTO_ODOO_DOCS_KB_PATH` or an explicit vault path is available before implementation-ready framework/user-flow guidance, while preserving the non-authority boundary: Obsidian still does not decide project schema, verification, review, queue resolution, completion, or upstream freshness. The validator now requires indexed user-manual raw/slim coverage and slim non-authority warnings. |
| ST-P1-53 | Obsidian labeling/index lane audit | medium | User request on 2026-06-10 after C-to-Z vault migration exposed standalone-looking notes and Inbox/Projects migration conflicts; `plans/OBSIDIAN_LABELING_INDEX_REVIEW_2026-06-10.md`; `docs/OBSIDIAN_INTEGRATION.md`; `scripts/audit-obsidian-vault.py`; `scripts/knowledge-notes.py` | complete_contract | Obsidian relationship checks now distinguish three lanes: curated AI_AUTO notes managed by `AI_AUTO_INDEX.md` plus generated hubs, domain KB folders managed by folder-local indexes and scoped validators, and large reference baselines managed by baseline indexes plus raw/slim parity validators. `audit-obsidian-vault.py` gives a read-only pre-cleanup report for curated note counts, generated hubs, top-level KB/reference folders, and Inbox/Projects conflicts classified by exact duplicate, same `source_hash`, same `repeat_key`, or manual-review-needed. The audit is evidence only and does not authorize deletion, merge, overwrite, verification, review, or project behavior changes. |
| ST-P1-54 | Odoo local registry-load validation harness (warm-base) | high | This session (2026-06-10): built and proved a local pre-push validation harness against parity-pinned Odoo 19 (community+enterprise fetched from odoo.sh build `33323752`); `plans/ODOO_LOCAL_VALIDATION_HARNESS_2026-06-10.md`, `plans/ODOO_BUILD_ERROR_ANALYSIS_2026-06-10/`, `templates/domain-packs/odoo/validation-harness/`, `templates/domain-packs/odoo/verify-patterns.md` | advisory_contract | Shipped reusable reference tooling in the odoo domain pack: `docker-compose.validate.yml` + `Dockerfile` (auto python deps), `validate-odoo.sh` (stateless full/single), `prepare-base-db.sh` (regenerable warm base = full module set + locale baseline), `validate-warm.sh` (clone + `-u`, ~tens of s), `setup_company.py` (1-lite fiscal-country baseline), plus a pre-push gate pattern. Operationally realizes the `ST-P1-51` fail-closed `-u` gate; proved end-to-end on JW odoo (19 custom modules + enterprise install/load PASS; warm validate ~95s; pre-push sim PASS). Onboarding reference tooling (a project adopts it and points at its own parity-pinned source), not a mainline fail-closed caller, so `advisory_contract`. Enterprise is mounted-not-baked; reliability needs odoo.sh module-set + point-release parity; data-baseline-dependent errors stay best-effort with odoo.sh staging as final validator. |
| ST-P1-55 | OCA static-lint commit tier for the odoo domain pack | medium | `ST-P1-54` open-source analysis (2026-06-10) + Plan-subagent review; `templates/domain-packs/odoo/commit-tier/` (`.pre-commit-config.yaml`, `.pylintrc`, `README.md`), `templates/domain-packs/odoo/verify-patterns.md`, `templates/domain-packs/odoo/review-checklist.md` | advisory_contract | Adopted off-the-shelf OCA static tools as the commit tier (no custom lint — Article 1.1): `OCA/odoo-pre-commit-hooks@v0.2.22` (manifest/XML/CSV/PO hygiene, duplicate id/field, deprecated nodes) + `OCA/pylint-odoo@v10.0.2` (Odoo 19.0 odoolint; `--disable=all --enable=odoolint` suppresses plain-pylint false positives when Odoo is not importable). Documents the commit→push→merge tiering. Honest scope: a static pre-filter only — **does NOT catch T2 view-inheritance (0/8), the bulk of T1 schema renames, T4/T5/T7, or cross-file T6**; the `validation-harness/` registry-load push tier (`ST-P1-54`) stays definitive. Opt-in reference config (no onboarding installs), advisory_contract not a mainline caller. The OpenUpgrade T1 rename cheat-sheet stays a separate Later-Gated item. |
| ST-P1-56 | Review-gate efficiency: provenance skip + cadence-sized panel | high | 2026-06-11 user req (build-error/AI-review churn loss); `plans/AI_AUTO_REVIEW_GATE_EFFICIENCY_PLAN_2026-06-11.md`; measured R0 baseline 531 verdicts/33d, 61% re-runs<15min (archive-inclusive), no review provenance; `scripts/summarize-ai-reviews.sh`, `scripts/review-gate.sh`, `scripts/collect-review-context.sh`, `scripts/verify-machinery.sh`, `scripts/test-review-summary.sh`, `docs/WORKFLOW.md` | complete_contract | `REVIEW_TARGETED_RECHECK` defaults to 1 (fails closed on scope expansion via `REVIEW_TARGETED_RECHECK_SCOPE_OK=0`). `summarize-ai-reviews.sh` records a working-tree-inclusive provenance hash in `.omx/reviewer-state/approved-provenance.env` ONLY on proceed + normal trust; `review-gate.sh` skips the AI panel on a byte-identical match (carrying the prior verdict) and fails open to a full review on any change, flag mismatch (D.4), or `*.disabled` reviewer (D.9). `verify.sh` always runs; archive only touches `.omx/review-results/` so provenance survives (D.8). `REVIEW_INTEGRATION_ONLY=1` forces a mandatory light cross-task-interaction pass (panel/trust unchanged) when combining approved diffs; `REVIEW_DECISION_GATE=1` disables every reduction for the full unanimous PR/pre-merge panel. The shared provenance block is byte-identical across both scripts (static contract test). Honest scope: delta-scoped review (review only `approved_head..now`) is **deferred** — it needs a diff base in `collect-review-context.sh`, so every non-exact case currently fails open to a full review; the original plan's single-reviewer integration was implemented as a panel-intact light pass instead (trust logic untouched). Template-synced under `AI_AUTO_TEMPLATE_VERSION` `2026.06.11.0`. |
| ST-P1-57 | Odoo validate-full (test+demo) pass & deps parity (domain-pack reference) | high | 2026-06-11 user req (build-error loss); `plans/ODOO_VALIDATE_FULL_AND_DEPS_PARITY_PLAN_2026-06-11.md`; extends `ST-P1-54` warm harness; `templates/domain-packs/odoo/validation-harness/` (`prepare-base-db.sh`, `validate-full.sh`, `gen-requirements.sh`, `README.md`), `plans/benchmarks/20260611-odoo-validate-full-budget.md` | advisory_contract | Adds the slow-class local tier on top of the push-tier registry-load gate: `prepare-base-db.sh ODOO_WITH_DEMO=1` builds a `base_demo` (keeps demo data); `validate-full.sh` runs a scoped post-install **test pass** (`-u $SCOPE --test-enable --test-tags=/mod` on the no-demo `base`, reverse-dep closure, rc + log-grep) and a **demo pass** (`-u $SCOPE` on the full-set `base_demo`), with git-diff **sub-routing** (demo pass only on `demo/` change, test pass on code change; a `demo/` change also runs the test pass to re-validate module load, and since `-u` does not reload demo it fails closed unless `ODOO_DEMO_REBUILD=1` rebuilds base_demo to validate the changed demo data) and a `MAX_FULL_SCOPE` reverse-dep cap. `gen-requirements.sh` generates/`--check`s a root `requirements.txt` from manifest `external_dependencies.python` (M\\R=error breaks odoo.sh, R\\M=warn) — closes the local-pass/odoo.sh-fail dep asymmetry. Measured findings: `-u` does NOT reload `demo/` data (changed demo needs a `base_demo` rebuild); a partial `-i` on an empty DB false-fails on cross-module comodels so the demo pass stays full-set; `createdb -T` clone ~0.8s + explicit `odoo-bin` path already give source parity, so **click-odoo is measured-and-rejected** (DR-B1). Budget: validate-warm ~95s (push), validate-full ~175s (on-demand) << one odoo.sh build round-trip; brings locally-catchable build-error classes to ~90% of the 41. Reference tooling in the domain pack (advisory_contract); the project's concrete `base_demo`/`requirements.txt`/hook stay in `jw_dev/00. DATA/` (not committed). Floor: P1 source-pin + real-data/migration-result stay staging-only. |
| ST-P1-58 | Multi-terminal concurrency safety (worktree-per-terminal + same-tree session lock) | high | 2026-06-11 user req (frequently runs several terminals on ONE project → writer races + review-gate flakiness under concurrent sessions, see review-gate-principal-via-launcher-evidence); `tools/ai-worktree`, `scripts/session-lock.sh`, `scripts/review-gate.sh`, `scripts/verify.sh`, `scripts/install-global-files.sh`/`automation-doctor.sh`/`bootstrap-ai-lab.sh`, `docs/GLOBAL_TOOLS.md` | complete_contract | New global helper `ai-worktree` (+ `aiwt` shell fn): create-or-enter a per-terminal git worktree (`aiwt <name>` → `git worktree add -b wt/<name>`), idempotent enter, `--list`/`--remove` with guards (refuses current/primary), prints path-only on stdout for the cd convention (mirrors `ai-home`). Each worktree has its own working tree AND its own gitignored `.omx/` → isolation by construction (operationalizes WORKFLOW.md Writer 격리). New `scripts/session-lock.sh` sourced by review-gate.sh + verify.sh: per-tree lock `.omx/state/session.lock`, soft-blocks a second LIVE session on the same tree (warns → points at `aiwt`; `AI_AUTO_ALLOW_SHARED_TREE=1` override), re-entrant for review-gate→verify nesting via exported `AI_AUTO_SESSION_ID`/session.json id, stale-lock reclaim by `kill -0` liveness, two separate worktrees never false-block. Functionally tested incl. live (a running verify holds the lock, a concurrent session is blocked, nesting re-entrant, holder undisturbed). Helper-link surfaces synced across install/doctor/bootstrap/GLOBAL_TOOLS (contract test). Deferred: `.omx` per-session sub-scoping (worktree model isolates `.omx` already). Template-synced under `AI_AUTO_TEMPLATE_VERSION` `2026.06.11.1`. |
| ST-P1-59 | Odoo harness concurrency: multi-session + multi-project conflict-free | high | 2026-06-11 user req (concurrent local validation conflicts even with git worktrees, because `00. DATA/harness` is a shared sibling — postgres volume/base/compose/port); `templates/domain-packs/odoo/validation-harness/` (`harness-lock.sh`, `harness-slug.sh` new; `prepare-base-db.sh`, `validate-warm.sh`, `validate-full.sh`, `validate-odoo.sh`); project-side mirror in jw_dev/00. DATA/harness (not committed) | advisory_contract | Within a project: clone names are now `val_$$_$(date +%s%N)` (pid+nanos, never collide) and a reader/writer flock (`harness-lock.sh`) lets concurrent `validate-*` run as READ while `prepare-base-db` rebuilds under a WRITE lock — no clone reads a half-rebuilt base, no double rebuild; the demo-rebuild path releases its read lock first (`harness_unlock`) to avoid a read→write self-deadlock. Across projects: `harness-slug.sh` derives a stable docker-safe slug (`h-<tail>-<cksum>`, space/Korean-safe) from the project path and exports `COMPOSE_PROJECT_NAME`, which namespaces the container, network, AND `odoo_pgdata` volume per project (no compose-file edit) + a per-project lock file — so two projects have fully separate postgres/volume/base/lock (proven: two slugs → `ai-cc-iso-a/b-db-1` + separate volumes, real `harness_odoo_pgdata` untouched). Adopting on an existing checkout = one base rebuild per project (regenerable cache). Serve-time single-port isolation deferred to imp3 (local serve+UI). Reference tooling (advisory_contract); concrete bases stay in jw_dev/00. DATA. |
| ST-P1-60 | tmux-integrated auto-worktree lifecycle (open-creates / close-removes-if-clean) | medium | 2026-06-11 user req (open a tmux window → auto worktree; close tmux → remove it; no manual `aiwt`); `tools/ai-tmux-worktree`, `scripts/install-global-files.sh` (`install_tmux_hooks` + `--install-tmux-worktree`), `docs/GLOBAL_TOOLS.md`, install/doctor/bootstrap link surfaces | complete_contract | `tools/ai-tmux-worktree create|remove|gc` drives `ai-worktree`: a tmux **window** (granularity forced by tmux 3.4 — session ids are empty at create/close, empirically verified) maps to worktree `<repo>-tmux-w<id>` on branch `wt/tmux-w<id>`. `after-new-window`/`after-new-session` create + `respawn-pane -c` lands the shell in it (idempotent via `@ai_wt_done`); `window-unlinked` removes it ONLY if clean — uncommitted OR unpushed (commits since the captured base, via a `~/.cache` sidecar) work is KEPT and logged, never `--force`d; `session-closed` runs a gc sweep. Opt-in: `--install-tmux-worktree` writes a managed `~/.tmux.conf` hook block (begin/end markers, idempotent, preserves user config) + `AI_AUTO_TMUX_WORKTREE=1` arms it + the start dir must be an AI_AUTO project; recursion-guarded; `AI_AUTO_TMUX_WORKTREE_KEEP=1` disables removal. Functionally tested incl. real tmux: create/respawn/idempotent, clean-remove, uncommitted-KEEP, unpushed-commit-KEEP. Composes with `ai-worktree`/`aiwt`. |
| ST-P1-61 | KB bidirectional retrieval — capture/push + domain-gated auto-retrieval (all stages shipped) | medium | 2026-06-12 user req (continual-learning trend → memory/retrieval audit: vault WRITE-mostly, PULL orphaned, capture stalled) + 3-way AI council (claude/codex gpt-5.5/gemini); `plans/AI_AUTO_KB_BIDIRECTIONAL_RETRIEVAL_PLAN_2026-06-12.md` (uncommitted, plan-files-stay-local convention); `tools/{ai-project-profile,knowledge-capture,knowledge-retrieve,ai-kb-retrieval-hook}`, `scripts/{install-automation-template,obsidian-autopush,install-global-files}.sh`, install/doctor/bootstrap/GLOBAL_TOOLS surfaces, `templates/domain-packs/odoo/{WORKFLOW,AGENTS.patch}.md` | complete_contract | Two-speed engine: capture grows CONTENT (auto, project-local), router gains COVERAGE by deliberate registration (Odoo first). **Stage 0 (shipped)**: `tools/ai-project-profile` detects domain (Odoo=`custom-addons/`+`__manifest__.py`) → machine-local `.omx/project-profile.json` `{domain,version,addons,detected_by}`; aiinit writes it for detected Odoo projects (no-op otherwise); odoo domain pack gains a "consult slim KB first, KB advisory — repo evidence wins" retrieval rule. Advisory/guidance only, no runtime change. **Stage 1A+1A.2 (shipped, 2026.06.12.1)**: `tools/knowledge-capture` harvests single-line Lore-`Finding:`-trailer commits → sanitized local drafts (reuse-test gate {rule,evidence,scope} drops the rest = junk-vault defence; secret/path-redacted; `local_private` unless `Finding-Share: shareable`); a behavior contract test locks the gate/dedup/redaction. `obsidian-autopush.sh` preflight fails loud on non-writable vault + warns on sibling-drive drift (/mnt/c-vs-/mnt/z class). Capture local+opt-in; push stays user-triggered. Review-verdict capture deferred until verdicts carry a finding-trailer convention. **Stage 2+1B (shipped, 2026.06.12.2)**: `tools/knowledge-retrieve` is the PULL worker (keywords → registered domain slim index, matched on topic names AND slim headings → capped slim pointers, never raw, advisory, fail-graceful); `tools/ai-kb-retrieval-hook` is a `UserPromptSubmit` hook that injects that block only when BOTH gates pass (profile=registered domain AND prompt-keyword), fail-open + opt-in (`install-global-files.sh --install-kb-retrieval-hook` idempotently merges into `~/.claude/settings.json`). Behavior contract tests lock retrieval matching, gates, and fail-open/graceful posture. Honest scope: registry is Odoo-only (other domains earn a slot when they have a rich KB); retrieval keyword-recall is heading-based, not semantic. Template-synced `2026.06.12.2`. |
| ST-P1-62 | Odoo cross-addon inherited-model field collision: advisory validate-full nudge | high | feedback queue `odoo:post-install-gap-field-semantic-collision` (jw_dev, odoo.sh build 2026-06-15 post_install fail, commits 606f543/6a57059); report `2026-06-15-odoo-post-install-gap-sale-purchase-billing-code-collision.md`; adversarial verify 2026-06-28; AI council D2 (Codex + 2 subagents) | advisory_contract | Shipped `domain-packs/odoo/validation-harness/check-inherited-field-overlap.py` (advisory, always exit 0, diff-scoped): flags a `(inherited model, field name)` pair written by >=2 CHANGED addons — the cross-addon BEHAVIORAL collision class warm registry-load stays green on. Wired into the odoo `hooks/pre-push` as a non-blocking advisory (always exit 0, never blocks); documented in `verify-patterns.md` + `review-checklist.md` with `validate-full.sh` (post-install tier) as the oracle and module-prefixed field names as the durable fix. NOT a duplicate-field lint (Article 1.1): the single-addon legal override is never flagged; the council chose the >=2-addons-SAME-field-name signature (Codex refinement) so it is rare/high-precision, neutralizing the noise objection. Regression-fixtured in `verify-machinery.sh` (same-field-name 2 addons → flag at rc0; `--strict` → rc1; different field names → silent). |
| ST-P1-63 | Pre-push Odoo validation liveness + stale-fetch doc note (UX only) | low | feedback queue `pre-push:odoo-validation-looks-hung-and-concurrent-race` (jw_dev, 2026-06-22); adversarial verify 2026-06-28 | complete | Shipped a liveness note in `validation-harness/README.md` (Pre-push gate) + a start-banner line in `validate-warm.sh`: the `--log-level=warn` run is quiet ~tens-sec to ~2min (do not interrupt; an interrupted clone DB self-heals), and a mid-validation `git fetch` may show a stale `origin` — confirm the push outcome via `git rev-list`, not the interim fetch. No new hook and no concurrency lock: concurrency is already safe via `harness-lock.sh` shared READ lock + per-PID clone DBs, and the originally-proposed EXCLUSIVE lock was rejected as a stale-lock/deadlock regression. Start banner + result line already existed; only the time hint + stale-fetch guidance were added. |
| ST-P1-64 | doc-budget completion-base-ref auto-wiring in gate launcher | medium | feedback queue `template-proposal:doc-budget-codeonly-zero-guidance-exempt` (jw_dev, NEW_JS_dev commit 5b45d97 / review-verdict-20260625T173628); adversarial verify 2026-06-28; AI council D1 (Codex + 2 subagents, unanimous) | complete_contract | Shipped: `ai-principal-runtime.sh record-launch` records `launch_base_commit` (HEAD at run start); new `ai-principal-runtime.sh completion-base` re-emits it ONLY after the review-gate launcher-evidence guard (execution_mode/source/workspace match, non-symlink) plus an ancestor-of-HEAD check, so it can never silently narrow hard-fail to a non-ancestor base. `verify-machinery.sh` injects it one-shot into the doc-budget call (never exported → no verify-pytest leak); an explicit env override wins; no/invalid evidence falls back to today's branch-cumulative measurement. doc-budget.sh logic UNCHANGED. Regression fixture (home + template) proves: prior-run debt downgrades to a warning while small current work passes, this-run bloat after launch still HARD-FAILS (split-to-evade closed), and 4 tamper cases (no launch_base, non-ancestor, wrong workspace, no evidence) fail closed. The 0-line/warning-only exemption was rejected as a loophole. Template-synced `2026.06.28.1`. |
| ST-P1-65 | Checksheet deterministic oracle runner — harden + wire (generic runner); 2-lane oracle later | high | QC verification-checksheet pipeline; `scripts/checksheet-run.{py,sh}`, `tests/test_checksheet_run.py` (ponytail 5-pattern port); `plans/AI_AUTO_CHECKSHEET_RUNNER_DESIGN_2026-06-26.md`; Phase-2 adversarial panel (Codex + necessity/oracle/sequencing subagents), unanimous 2026-06-28 | later_gated | FACT CORRECTION: the PoC is ALREADY merged to main (`scripts/checksheet-run.{py,sh}` + tests at HEAD) — the earlier "unmerged on wt/tmux-w5" was stale drift. It is caller-less and NOT template-synced (no `templates/automation-base/` copy = off-manifest shadow). Panel-unanimous, SPLIT into two stages. NEAR-TERM (cheapest correct move; home = mainline `scripts/`, sibling of `self_demo_contracts.py`): fix the drift — template-sync runner+tests (PATCH_NOTES + version bump) and add a SCOPED opt-in caller that runs ONLY when a checksheet artifact is in the diff (never a no-op on every push); HARDEN fail-closed — JSON-schema-validate the checksheet, and make unknown-oracle / missing-target / selftest-fail all exit!=0; ENFORCE the omission-forcing completeness gate (a declared expected-item set bound to the change surface must fail closed on any missing result line — today the runner only counts, so 누락강제 is DECORATIVE), and make the `implicit` field ASSERT, not just print. selftest + LLM-exclusion are already sound (the structural answer to the `echo $?` masking false-pass class). LATER-GATED (the actual oracle = the documented bottleneck; do NOT present the merged generic runner as having solved it): the 2-lane design is UNBUILT ("named only" in the design doc) — promote ONLY when one real oracle plugin decides a real artifact. DB-delta (intended `model.field` whitelist + ignore-set, all-else=FAIL, + `(action × principal)` matrix) is the strongest lane → build first; Excalidraw-as-oracle is REJECTED (groupId/containerId=0 makes role auto-classification impossible, and a sketch is intent-not-truth) → use Playwright `toMatchAriaSnapshot` after role-tag elevation. |
| ST-P1-66 | Odoo view-inheritance gate — CI registry-load PARITY gate (residual only) | high | Later-Gated; queue `odoo:view-inheritance-registry-validation-required`; `plans/ODOO_VIEW_INHERITANCE_REGISTRY_VALIDATION_FEASIBILITY_2026-06-09.md`; builds on shipped `ST-P1-51` (guidance) + `ST-P1-54` (warm registry-load harness); Phase-2 adversarial panel, unanimous 2026-06-28 | later_gated | Panel-unanimous SCOPE CUT + SPLIT. Keep ONLY the "CI registry-load parity gate" residual; MOVE the Tier-1 static-lint slice OUT to `ST-P1-68` (it is the same OCA static surface and provably catches 0/8 of the T2 view-inheritance class — folding it in falsely implies a static lint is part of the view-inheritance oracle). The registry-load (`-u <changed> --stop-after-init`, product-judges-itself) is the strongest oracle of the QC set, BUT the CI slice is largely a re-platform of the SHIPPED local warm harness (`ST-P1-54`); the only genuine residual is asserting odoo.sh module-set + point-release PARITY at CI fidelity. Approve-conditions: compute changed modules + reverse-dependency closure before `-u` (reuse `ST-P1-57` `MAX_FULL_SCOPE`); PROVE odoo.sh source/module-set/point-release parity and fail-closed BLOCKED (never PASS) when parity is unconfirmed — a stale-parity local pass is a silent false oracle; enterprise mounted-not-baked; unavailable = NOT VALIDATED. CORRECTION: this is downstream-CI / domain-pack tooling — it canNOT be a mainline `verify.sh`/`review-gate.sh` caller (this repo has no Odoo); the prior "verify with verify.sh" was wrong. |
| ST-P1-67 | Synthetic data-baseline for data-dependent validation (sandboxerp-style) | low | Later-Gated; harness best-effort data-baseline gap (T5 demo / T7 NOT NULL); Phase-2 adversarial panel, unanimous 2026-06-28 | later_gated | Panel-unanimous: do NOT build now — keep DORMANT. It is a FIXTURE, not an oracle; a growing synthetic seed manufactures "passes on our synthetic data" false confidence and drifts from real customer/migration data. T5 is already exercised by the SHIPPED `ST-P1-57` `base_demo` demo pass; T7/data-dependent errors are explicitly best-effort with odoo.sh staging as the FINAL validator. sandboxerp is unofficial (maintenance + non-determinism risk). REOPEN trigger: >=2 NEW recurring T5/T7 false-negatives logged that `base_demo` + `ODOO_DEMO_REBUILD=1` + `validate-full` provably miss. If ever built: ONE deterministic regenerable seed recipe (fixed RNG, content-addressed), no PII, no hand-maintained state, parity-pinned, results downgrade-confidence-only (never installable-evidence; staging stays terminal). CORRECTION: domain-pack tooling, not a mainline verify caller. |
| ST-P1-68 | OCA static-lint commit tier: advisory-by-design (enforcement = downstream opt-in only) | medium | Decision on the commit-tier slice shipped as `ST-P1-55`; `templates/domain-packs/odoo/commit-tier/`; absorbs the Tier-1 static-lint slice split out of `ST-P1-66`; Phase-2 adversarial panel, unanimous 2026-06-28 | advisory_contract | Panel-unanimous DECISION: CLOSE as advisory-by-design; do NOT enforce on mainline. The OCA commit-tier (`OCA/odoo-pre-commit-hooks` + `OCA/pylint-odoo`, no custom lint per Article 1.1) is already correctly shipped as opt-in reference (`ST-P1-55`). Enforcing it manufactures "commit-tier-green = safe" FALSE CONFIDENCE — it catches NONE of T2 view-inheritance / bulk T1 / T4/T5/T7 / cross-file T6 (the `validation-harness/` registry-load push tier stays definitive) — while loading pinned-tool/network/pre-commit-venv maintenance + worktree-corruption/oscillation risk onto the gate for ~zero oracle gain. Enforcement is justified ONLY as a downstream OPT-IN installer path when a specific Odoo project explicitly requests it after recurring static-detectable errors: isolated `pre-commit install`, pinned OCA versions, odoolint-only, no autofix, framed so a clean static pass is NEVER installable/pass evidence. CORRECTION: cannot be wired into this repo's `verify.sh` (no Odoo here); the only enforcement lever is the downstream installer + git hooks. Also absorbs `ST-P1-66`'s Tier-1 static-lint slice. |
| ST-P1-69 | review-gate session-lock contention sentinel (shared-worktree concurrency) | medium | feedback queue `review-gate:session-lock-preemption-misread-as-verify-failure` + `review-gate:scope-whole-worktree-vs-commit-shared-checkout` (2026-06-28, jw_dev concurrent sessions); adversarial panel (Codex + necessity/safety/impl subagents), unanimous 2026-06-28; implementation adversarially re-reviewed (Codex + 2 subagents) | complete_contract | Shipped (`2026.06.28.4`). `session-lock.sh`'s LIVE-foreign-holder branch returns sentinel **75** (EX_TEMPFAIL) instead of 1; stale-reclaim / own re-entrant session / `AI_AUTO_ALLOW_SHARED_TREE=1` all stay 0. `review-gate.sh`'s own acquire (before verify) and standalone `verify.sh` capture the code with an or-clause (both are `set -euo pipefail`, so set -e cannot exit before capture) and surface 75 as a non-blocking "deferred — re-run or use `aiwt`" (`exit 75`, no verdict). A 75 is NEVER a `proceed`/`proceed_degraded` (verification never skipped) and NEVER a `verify_failed`/blocked verdict; a genuine verify failure still blocks. The implementation adversarial panel caught and removed a corroboration guard at the red-signal branch that could false-defer a REAL failure under shared-tree mode (no true-positive path under the gate anyway, since nested verify is re-entrant) — so the gate's red-signal branch simply blocks any nonzero, and real contention is handled only at the gate's own acquire. It also caught a `set -e` capture bug (fixed with an or-clause). Mirrored `session-lock.sh` + `review-gate.sh` to `templates/automation-base`; `verify.sh` is source-only. verify-machinery fixture (home + template): live foreign holder → 75, own → 0, stale → 0, shared-override → 0. ST-P1-70 (Slice B) stays later_gated/dropped. |
| ST-P1-70 | review-gate worktree-scope vs review-target (Slice B disposition) | low | feedback queue `review-gate:scope-whole-worktree-vs-commit-shared-checkout`; same adversarial panel, unanimous 2026-06-28 | later_gated | Panel-unanimous: the proposed AUTO out-of-scope downgrade is REJECTED — path prefixes cannot distinguish "a sibling session's churn" from "this session's own forgotten/unstaged related file", so downgrading would mask a real commit-readiness problem. Most of the ask ALREADY exists and is fail-safe: the untracked-material guard already auto-scopes to the tracked changed-file diff and reports out-of-scope files as non-blocking (`collect-review-context.sh` `write_untracked_review_guard`, `auto_changed_scope`); the Tree Churn Audit is already `report_only`; sibling-live-session detection already exists (`session-lock.sh`). The provenance hash is whole-tree BY DESIGN (security control: any churn → full review, never a false skip — DR1 / D.4 skip-fingerprint) and is byte-identical-duplicated across 4 files with a parity test, so narrowing it = skip-bypass — DO NOT touch. Root remedy for shared-worktree concurrency = one worktree per session (`ST-P1-58` ai-worktree/`aiwt`, already the documented preference + soft-block at the lock). later_gated residual = ONLY an opt-in EXPLICIT-target review mode (Codex) where provenance hashes HEAD + an explicit pathspec AND includes the target declaration in the hash (so a prior approval for one target cannot be reused for another) — a separate feature, not needed for the observed incidents. |
| ST-P1-71 | odoo serve.sh resource-watchdog flags (WSL restart-loop fix) | high | feedback queue `serve:wsl-resource-limits-watchdog-restart-loop` (2026-06-28, jw_dev-tmux-w5 local serve); adversarial panel (Codex + safety/correctness subagents), converged 2026-06-28; implementation adversarially re-reviewed (Codex + 2 subagents) | complete_contract | Shipped (`2026.06.28.5`). `validation-harness/serve.sh` runs odoo-bin (lines ~84-91) with the container odoo.conf defaults (`limit_time_real=120s`, `limit_memory_hard≈2.5GB`); on WSL2 the first 177-module+enterprise registry load (~150-550s) + first /web asset compile overruns the TIME watchdog → Odoo force-restarts ("Initiating server reload") every ~2-3min → /web stuck "loading". Field-verified that disabling the limits gives 0 restarts + /web 200. Panel-CONVERGED refinement of the raw "all-5-flags=0" proposal: pass 5 flags via host-side `-e SHORT="${ODOO_SERVE_*:-default}"` then `--flag="$SHORT"` INSIDE the single-quoted `bash -c` block (critical: `${ODOO_SERVE_*}` written inside the quotes expands in the CONTAINER where it is unset → dead override). Defaults: `--limit-time-real=0 --limit-time-cpu=0` (time watchdog OFF — the real loop cause, no SLA in manual verify) and `--max-cron-threads=0` (hygiene; cron-dependent UI flows are the documented override exception), BUT memory stays HIGH-BUT-CAPPED not 0: `--limit-memory-soft≈6GiB --limit-memory-hard≈8GiB` (under the 11G WSL VM). Rationale: compose has NO `mem_limit`/cgroup cap (`docker-compose.validate.yml:15-29`), so `--limit-memory-hard=0` removes the LAST guard before the WSL OOM killer reaps the whole VM (host has a documented OOM-crash history) — strictly worse than a recoverable single-process restart. `ODOO_SERVE_LIMIT_{TIME_REAL,TIME_CPU,MEMORY_SOFT,MEMORY_HARD},ODOO_SERVE_MAX_CRON_THREADS` numeric pass-throughs (set to 0 = explicit unlimited escape hatch, or a stock value e.g. 120 to restore). Add a wait-guidance `echo` BEFORE the `exec` (post-`exec` polling is impossible). Optional backstop: a `mem_limit` on the compose odoo service (cgroup kills only the container, not the VM). Scope: odoo domain pack, serve-only (batch `validate-*`/`prepare-base-db` `--stop-after-init` tiers untouched). DID require a version bump — the adversarial impl panel (Codex) caught that `scripts/check-template-version.sh` bump-on-change covers `templates/domain-packs/` too (not just automation-base), so a domain-pack change without a bump would BLOCK the gate; bumped 2026.06.28.4→.5 + PATCH_NOTES. Documented in serve.sh header Env block + `verify-patterns.md` serve section + `validation-harness/README.md`. No fixture (Docker/WSL runtime, not unit-testable; field-verification 0-restarts/200 is the evidence). Impl panel also noted serve is threaded (workers=0) so the memory cap reloads via the watchdog and the `.wslconfig` ceiling is the ultimate backstop — wording made precise. |
| ST-P2-01 | External tool install evaluation | medium | `plans/AI_AUTO_SMALL_TOOL_ADOPTION_REVIEW_2026-05-28.md`, `plans/AI_AUTO_EXTERNAL_TOOL_INSTALL_EVALUATION_2026-05-29.md` | installed_required | `shellcheck` is installed and required at warning severity; `hyperfine` is installed and remains observational benchmark capture. |
| ST-P2-02 | ShellCheck warning triage | medium | `plans/AI_AUTO_EXTERNAL_TOOL_INSTALL_EVALUATION_2026-05-29.md` | complete | Warning-severity findings are clean and enforced by `verify.sh`; info/style findings remain optional cleanup candidates only. |
| ST-P2-03 | Fast Python static-analysis gate before pytest (diagnostics-before-heavy-gate slice of the LSP pattern) | low | 2026-06-11 trend scan (OpenCode LSP-diagnostics pattern) + #1 deep review + 3-way discussion (claude/codex gpt-5.5/gemini) + coverage measurement | reviewed_deferred | **Reviewed & deferred — value real but modest.** 3-way verdict: all agreed value is NARROW (cheap broken-code detector, not a quality system), tight scope (`E9,F` or tighter `E9,F821,F811`), defer types (mypy/pyright = maintenance sinkhole on a shell/python hybrid), PIN the version. Split: codex=implement-tightest, gemini=skip (lean control plane, baseline already covered). Measured tiebreaker: ~20 Python files (.py 15 + extensionless 5), mostly subprocess-exercised → ruff only adds untested-BRANCH typo/syntax catches = real-but-small. Net: nice-to-have, not need-to-have; 211 tests + shellcheck already hold the safety baseline. **If ever pursued**, the minimal recipe is `ruff check --select E9,F821,F811` (pinned), reusing verify-machinery's shebang discovery for the 5 extensionless python tools, `F401/F841` only after observing noise. Not pursued now. |
| ST-P2-04 | Package AI_AUTO as a Claude Code / Codex plugin (MCP-exposed tool subset) for desktop/IDE-client use | medium | 2026-06-11 trend scan + user goal (use AI_AUTO combined with Codex Desktop / Claude Code, not just the shell CLI); current state: 29 shell tools called via `~/bin`/Bash (works for CLI runtimes but not GUI clients); existing stance `AI_RUNTIME_ADAPTERS.md:75` "MCP/tool connector는 현재 계약 밖"; one prior MCP (omx_memory) was transport-closed-flaky with a local fallback | candidate (scope-gated) | **Reframed by the desktop-client goal:** for CLI (Claude Code terminal, codex CLI) the shell tools already work via Bash → MCP not needed. For DESKTOP/GUI clients (Codex Desktop, Claude Desktop) MCP is the native tool surface. 2026 ecosystem is ready: Codex CLI is an MCP server since v0.117.0 (Mar 2026) + Codex plugins (Jun 2026); **Claude Code plugins bundle MCP servers AND LSP servers** — so this can ship together with the `ST-P2-03` LSP slice as one "AI_AUTO plugin". Honest scope: AI_AUTO is a git-tree+shell control plane, so expose a high-value SUBSET first (status/query, `knowledge-collect`, `ai-auto-template-status`, …) and keep heavy shell-bound ops (verify, review-gate, promote) in the CLI; design for graceful degradation (the omx_memory MCP flakiness). Effort scales with scope — gate on confirming desktop-integration is a real goal + which subset. |
| ST-P2-05 | Visual agent/workflow builder for pipeline visibility | low | 2026-06-11 trend scan (Langflow/Dify/n8n drag-and-drop agent pipelines) — #3 review | not_pursued | **Reviewed & declined.** Two forms: (a) a drag-and-drop *builder* directly CONFLICTS with AI_AUTO's deterministic/auditable/git-versioned principle and the explicit `standard_flow_preservation_policy` (parallel-replacement guard) — anti-pattern, do not build; (b) read-only *visualization* is already largely covered by `PLANNING_VISUALIZATION_GUIDE.md` + excalidraw templates + the planning-visual gate (`ST-P1-13/36/37`). Residual sliver = a static diagram of the verify→review-gate→promote pipeline itself (docs nicety, linear flow, low need). Net: no capability gap; not pursued. |

There is no separate P3 backlog in this Ralph branch. The formerly non-active
workflow promotion items are now tracked explicitly. `ST-P1-07` through
`ST-P1-09` and `ST-P1-13` through `ST-P1-19` are operational-clear.

Runtime GStack installation and parallel sprint execution are intentionally
excluded from active and later-gated TODOs. The useful GStack parts are its review
vocabulary, planning lenses, quality scoring, and evidence formats; adopting
its daemon/runtime or sprint execution model would add authority overlap and
operating surface without clear benefit for this repository.

## Active TODO Classification

The current active TODO report excludes runtime GStack adoption and parallel
sprint execution, and uses these buckets:

1. Immediately executable TODOs: none. The TODO report wrapper, process cleanup
   runtime fixture, and diff-scope review-gate consumption are implemented.
2. Structural-audit TODOs not complete: none. Current SA items are closed by
   scoped contracts/evidence or explicitly outside active scope.
3. Small-tool active TODOs: none. `ST-P1-01` is display-only complete,
   `ST-P1-03` and `ST-P1-04` are implemented, `ST-P1-05` is complete for
   observe-mode capture, `ST-P1-06` has a deterministic runtime fixture,
   `ST-P1-07` through `ST-P1-09` and `ST-P1-13` through `ST-P1-19` are
   operational-clear. `ST-P1-22` (cross-runtime model-class routing, Phases
   0-2), `ST-P1-23` (principal misdetection fix), `ST-P1-24` (doc-budget
   guidance-vs-content scoping), `ST-P1-25` (review verdict parser
   prompt-echo hardening), `ST-P1-26` (agy prompt-only large-prompt
   placeholder guard coverage), `ST-P1-27` (archive double-confirm
   deletion safety default), `ST-P1-28` (Ralph completion-condition drift
   guard), `ST-P1-29` (untracked review scope allowlist), `ST-P1-30`
   (Obsidian shareable-only auto-push from home), `ST-P1-31` (rule-based
   shareable auto-promotion), `ST-P1-32` (Guidance Stage 2 Slice A AGENTS
   budget restore), `ST-P1-34` (large reference baseline storage pattern,
   odoo #6 reusable part), and `ST-P1-21` (MicroWork contract and review-gate
   audit tooling), and `ST-P1-36`/`ST-P1-37` (planning visual-artifact gate:
   complexity-triggered structure/flow/optimizer proposal and
   UI-wireframe-required-for-layout), and `ST-P1-38` (spec↔code alignment gate
   inside the implement/review loop), and `ST-P1-41` (preserve standard framework
   business flow before custom UI) are now `advisory_contract` because their
   review-context audits are report-only and their planning guide is
   template-synced as of `2026.06.05.8`. `ST-P1-42` (doc-budget plan/spec
   filename-label convention) remains complete_contract; `ST-P1-39` and
   `ST-P1-40` (framework-native wireframe-structure and wireframe-authoring
   guidance kernels) are complete. The full 2026-06-04 JW odoo project-feedback
   intake (`ST-P1-36` through `ST-P1-42`, under
   `plans/AI_AUTO_PROJECT_FEEDBACK_VISUAL_SPEC_PLAN_2026-06-04.md`) is now
   implemented, with report-only audit rows labeled honestly. `ST-P1-20` (domain-pack retrospective closeout contract),
   `ST-P1-33` (Stage 2 guidance consolidation decision-boundary contract),
   `ST-P1-35` (reviewer first-pass read-only posture diagnostics/contract),
   `ST-P1-43` (dry-run-by-default feedback resolver), `ST-P1-44` (targeted
   review recheck scope boundary), and `ST-P1-46` (deterministic installed
   domain-pack refresh workflow) are complete_contract; `ST-P1-45` (detailed UI
   micro-plan before browser/CDP evidence) is `advisory_contract`; `ST-P1-47`
   (agent worktree registry/status helper) and `ST-P1-48` (framework UI
   vector-wireframe harness) keeps only the optional checker tool `later_gated`
   pending repeated triggering incidents or explicit implementation approval;
   its vector-wireframe guidance itself is template-synced. `ST-P1-51` (Odoo
   view-inheritance registry-validation gate, domain-pack guidance slice) is
   `advisory_contract`: the pack states the registry-validation rule and
   fail-closed `-u` gate. `ST-P1-54` (Odoo local registry-load validation harness,
   warm-base) then shipped that gate as reusable domain-pack tooling
   (`advisory_contract`), proved end-to-end on JW odoo; only an optional static
   lint and a hosted CI variant remain later-gated.
4. Policy-attention items are also blocking: complete-status notes that still
   mention non-active tooling, runtime surfaces, parity drift, or later
   execution are treated as not clear by `scripts/todo-report.py`.
5. Currently needed external-tool candidates: none. `shellcheck` is installed
   and required at warning severity; `hyperfine` is installed and remains
   observational. All other candidate installs are excluded or reference-only
   until a new explicit trigger reopens them.
6. Documentation cleanup TODOs: none from the former 9000-line aggregate budget.
   Guidance Stage 2 is later-gated only, not an active TODO.

## Current Status

This backlog is an execution artifact, not broad implementation approval. Active
TODO clearance is verified by `scripts/todo-report.py --fail-on-active`; the
current canonical report is clear only when both active and policy-attention
counts are zero.
Runtime GStack, parallel sprint execution, browser/session automation,
second-memory runtime, and broad external lint/security/counting tools are
excluded, not deferred. Guidance Stage 1 is complete; the former aggregate
guidance warning is resolved by separate primary/template budgets. `shellcheck`
and `hyperfine` are installed and documented; only `shellcheck` is a required
gate.
